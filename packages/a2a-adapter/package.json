{
  "name": "@trust-substrate/a2a-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "pnpm run build && tsc -p tsconfig.test.json && node --test dist-tests/tests/a2a-adapter/*.test.mjs",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true }); require('node:fs').rmSync('dist-tests', { recursive: true, force: true })\""
  },
  "devDependencies": {
    "@types/node": "^24.12.2"
  }
}
