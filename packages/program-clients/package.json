{
  "name": "@trust-substrate/program-clients",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "generate": "pnpm exec node ../../scripts/generate-program-clients.mjs",
    "compile": "pnpm run clean && tsc -p tsconfig.json && pnpm exec node ../../scripts/fix-program-client-dist-imports.mjs",
    "build": "pnpm run clean && pnpm run generate && tsc -p tsconfig.json && pnpm exec node ../../scripts/fix-program-client-dist-imports.mjs",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\""
  },
  "dependencies": {
    "@solana/kit": "6.8.0"
  }
}
