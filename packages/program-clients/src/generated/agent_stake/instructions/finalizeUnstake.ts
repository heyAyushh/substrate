/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { AGENT_STAKE_PROGRAM_ADDRESS } from "../programs";

export const FINALIZE_UNSTAKE_DISCRIMINATOR = new Uint8Array([
  11, 165, 26, 80, 219, 175, 236, 227,
]);

export function getFinalizeUnstakeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINALIZE_UNSTAKE_DISCRIMINATOR,
  );
}

export type FinalizeUnstakeInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountStake extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountStake extends string
        ? WritableAccount<TAccountStake>
        : TAccountStake,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizeUnstakeInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FinalizeUnstakeInstructionDataArgs = {};

export function getFinalizeUnstakeInstructionDataEncoder(): FixedSizeEncoder<FinalizeUnstakeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: FINALIZE_UNSTAKE_DISCRIMINATOR }),
  );
}

export function getFinalizeUnstakeInstructionDataDecoder(): FixedSizeDecoder<FinalizeUnstakeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinalizeUnstakeInstructionDataCodec(): FixedSizeCodec<
  FinalizeUnstakeInstructionDataArgs,
  FinalizeUnstakeInstructionData
> {
  return combineCodec(
    getFinalizeUnstakeInstructionDataEncoder(),
    getFinalizeUnstakeInstructionDataDecoder(),
  );
}

export type FinalizeUnstakeInput<
  TAccountOwner extends string = string,
  TAccountStake extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  stake: Address<TAccountStake>;
};

export function getFinalizeUnstakeInstruction<
  TAccountOwner extends string,
  TAccountStake extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: FinalizeUnstakeInput<TAccountOwner, TAccountStake>,
  config?: { programAddress?: TProgramAddress },
): FinalizeUnstakeInstruction<TProgramAddress, TAccountOwner, TAccountStake> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    stake: { value: input.stake ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("stake", accounts.stake),
    ],
    data: getFinalizeUnstakeInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeUnstakeInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountStake
  >);
}

export type ParsedFinalizeUnstakeInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    stake: TAccountMetas[1];
  };
  data: FinalizeUnstakeInstructionData;
};

export function parseFinalizeUnstakeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFinalizeUnstakeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), stake: getNextAccount() },
    data: getFinalizeUnstakeInstructionDataDecoder().decode(instruction.data),
  };
}
