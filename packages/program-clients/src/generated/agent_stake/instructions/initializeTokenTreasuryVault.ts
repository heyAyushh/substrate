/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findTreasuryTokenVaultPda } from "../pdas";
import { AGENT_STAKE_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_TOKEN_TREASURY_VAULT_DISCRIMINATOR = new Uint8Array([
  87, 142, 212, 58, 58, 9, 219, 210,
]);

export function getInitializeTokenTreasuryVaultDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_TOKEN_TREASURY_VAULT_DISCRIMINATOR,
  );
}

export type InitializeTokenTreasuryVaultInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountTreasuryVault extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountTreasuryTokenVault extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountTreasuryVault extends string
        ? ReadonlyAccount<TAccountTreasuryVault>
        : TAccountTreasuryVault,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountTreasuryTokenVault extends string
        ? WritableAccount<TAccountTreasuryTokenVault>
        : TAccountTreasuryTokenVault,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeTokenTreasuryVaultInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeTokenTreasuryVaultInstructionDataArgs = {};

export function getInitializeTokenTreasuryVaultInstructionDataEncoder(): FixedSizeEncoder<InitializeTokenTreasuryVaultInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_TOKEN_TREASURY_VAULT_DISCRIMINATOR,
    }),
  );
}

export function getInitializeTokenTreasuryVaultInstructionDataDecoder(): FixedSizeDecoder<InitializeTokenTreasuryVaultInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeTokenTreasuryVaultInstructionDataCodec(): FixedSizeCodec<
  InitializeTokenTreasuryVaultInstructionDataArgs,
  InitializeTokenTreasuryVaultInstructionData
> {
  return combineCodec(
    getInitializeTokenTreasuryVaultInstructionDataEncoder(),
    getInitializeTokenTreasuryVaultInstructionDataDecoder(),
  );
}

export type InitializeTokenTreasuryVaultAsyncInput<
  TAccountPayer extends string = string,
  TAccountTreasuryVault extends string = string,
  TAccountMint extends string = string,
  TAccountTreasuryTokenVault extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  treasuryVault?: Address<TAccountTreasuryVault>;
  mint: Address<TAccountMint>;
  treasuryTokenVault?: Address<TAccountTreasuryTokenVault>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeTokenTreasuryVaultInstructionAsync<
  TAccountPayer extends string,
  TAccountTreasuryVault extends string,
  TAccountMint extends string,
  TAccountTreasuryTokenVault extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: InitializeTokenTreasuryVaultAsyncInput<
    TAccountPayer,
    TAccountTreasuryVault,
    TAccountMint,
    TAccountTreasuryTokenVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeTokenTreasuryVaultInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountTreasuryVault,
    TAccountMint,
    TAccountTreasuryTokenVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    treasuryVault: { value: input.treasuryVault ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    treasuryTokenVault: {
      value: input.treasuryTokenVault ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.treasuryVault.value) {
    accounts.treasuryVault.value = await getProgramDerivedAddress({
      programAddress:
        "uJx2R2MHL7PEob6UPNz2DevGKpwd35fnKCrDQoavbtF" as Address<"uJx2R2MHL7PEob6UPNz2DevGKpwd35fnKCrDQoavbtF">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 114, 101, 97, 115, 117, 114, 121]),
        ),
      ],
    });
  }
  if (!accounts.treasuryTokenVault.value) {
    accounts.treasuryTokenVault.value = await findTreasuryTokenVaultPda({
      mint: getAddressFromResolvedInstructionAccount(
        "mint",
        accounts.mint.value,
      ),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("treasuryVault", accounts.treasuryVault),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("treasuryTokenVault", accounts.treasuryTokenVault),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeTokenTreasuryVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeTokenTreasuryVaultInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountTreasuryVault,
    TAccountMint,
    TAccountTreasuryTokenVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type InitializeTokenTreasuryVaultInput<
  TAccountPayer extends string = string,
  TAccountTreasuryVault extends string = string,
  TAccountMint extends string = string,
  TAccountTreasuryTokenVault extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  treasuryVault: Address<TAccountTreasuryVault>;
  mint: Address<TAccountMint>;
  treasuryTokenVault: Address<TAccountTreasuryTokenVault>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeTokenTreasuryVaultInstruction<
  TAccountPayer extends string,
  TAccountTreasuryVault extends string,
  TAccountMint extends string,
  TAccountTreasuryTokenVault extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: InitializeTokenTreasuryVaultInput<
    TAccountPayer,
    TAccountTreasuryVault,
    TAccountMint,
    TAccountTreasuryTokenVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeTokenTreasuryVaultInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountTreasuryVault,
  TAccountMint,
  TAccountTreasuryTokenVault,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    treasuryVault: { value: input.treasuryVault ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    treasuryTokenVault: {
      value: input.treasuryTokenVault ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("treasuryVault", accounts.treasuryVault),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("treasuryTokenVault", accounts.treasuryTokenVault),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeTokenTreasuryVaultInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeTokenTreasuryVaultInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountTreasuryVault,
    TAccountMint,
    TAccountTreasuryTokenVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeTokenTreasuryVaultInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    treasuryVault: TAccountMetas[1];
    mint: TAccountMetas[2];
    treasuryTokenVault: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: InitializeTokenTreasuryVaultInstructionData;
};

export function parseInitializeTokenTreasuryVaultInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeTokenTreasuryVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      treasuryVault: getNextAccount(),
      mint: getNextAccount(),
      treasuryTokenVault: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeTokenTreasuryVaultInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
