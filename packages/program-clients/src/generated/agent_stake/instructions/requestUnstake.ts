/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { AGENT_STAKE_PROGRAM_ADDRESS } from "../programs";

export const REQUEST_UNSTAKE_DISCRIMINATOR = new Uint8Array([
  44, 154, 110, 253, 160, 202, 54, 34,
]);

export function getRequestUnstakeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_UNSTAKE_DISCRIMINATOR,
  );
}

export type RequestUnstakeInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountStake extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountStake extends string
        ? WritableAccount<TAccountStake>
        : TAccountStake,
      ...TRemainingAccounts,
    ]
  >;

export type RequestUnstakeInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type RequestUnstakeInstructionDataArgs = { amount: number | bigint };

export function getRequestUnstakeInstructionDataEncoder(): FixedSizeEncoder<RequestUnstakeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REQUEST_UNSTAKE_DISCRIMINATOR }),
  );
}

export function getRequestUnstakeInstructionDataDecoder(): FixedSizeDecoder<RequestUnstakeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getRequestUnstakeInstructionDataCodec(): FixedSizeCodec<
  RequestUnstakeInstructionDataArgs,
  RequestUnstakeInstructionData
> {
  return combineCodec(
    getRequestUnstakeInstructionDataEncoder(),
    getRequestUnstakeInstructionDataDecoder(),
  );
}

export type RequestUnstakeInput<
  TAccountOwner extends string = string,
  TAccountStake extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  stake: Address<TAccountStake>;
  amount: RequestUnstakeInstructionDataArgs["amount"];
};

export function getRequestUnstakeInstruction<
  TAccountOwner extends string,
  TAccountStake extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: RequestUnstakeInput<TAccountOwner, TAccountStake>,
  config?: { programAddress?: TProgramAddress },
): RequestUnstakeInstruction<TProgramAddress, TAccountOwner, TAccountStake> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    stake: { value: input.stake ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("stake", accounts.stake),
    ],
    data: getRequestUnstakeInstructionDataEncoder().encode(
      args as RequestUnstakeInstructionDataArgs,
    ),
    programAddress,
  } as RequestUnstakeInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountStake
  >);
}

export type ParsedRequestUnstakeInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    stake: TAccountMetas[1];
  };
  data: RequestUnstakeInstructionData;
};

export function parseRequestUnstakeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestUnstakeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), stake: getNextAccount() },
    data: getRequestUnstakeInstructionDataDecoder().decode(instruction.data),
  };
}
