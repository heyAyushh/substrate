/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { AGENT_STAKE_PROGRAM_ADDRESS } from "../programs";

export const REQUEST_UNSTAKE_TOKEN_DISCRIMINATOR = new Uint8Array([
  159, 11, 149, 72, 86, 237, 131, 61,
]);

export function getRequestUnstakeTokenDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_UNSTAKE_TOKEN_DISCRIMINATOR,
  );
}

export type RequestUnstakeTokenInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountTokenStake extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountTokenStake extends string
        ? WritableAccount<TAccountTokenStake>
        : TAccountTokenStake,
      ...TRemainingAccounts,
    ]
  >;

export type RequestUnstakeTokenInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type RequestUnstakeTokenInstructionDataArgs = {
  amount: number | bigint;
};

export function getRequestUnstakeTokenInstructionDataEncoder(): FixedSizeEncoder<RequestUnstakeTokenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: REQUEST_UNSTAKE_TOKEN_DISCRIMINATOR,
    }),
  );
}

export function getRequestUnstakeTokenInstructionDataDecoder(): FixedSizeDecoder<RequestUnstakeTokenInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getRequestUnstakeTokenInstructionDataCodec(): FixedSizeCodec<
  RequestUnstakeTokenInstructionDataArgs,
  RequestUnstakeTokenInstructionData
> {
  return combineCodec(
    getRequestUnstakeTokenInstructionDataEncoder(),
    getRequestUnstakeTokenInstructionDataDecoder(),
  );
}

export type RequestUnstakeTokenInput<
  TAccountOwner extends string = string,
  TAccountTokenStake extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  tokenStake: Address<TAccountTokenStake>;
  amount: RequestUnstakeTokenInstructionDataArgs["amount"];
};

export function getRequestUnstakeTokenInstruction<
  TAccountOwner extends string,
  TAccountTokenStake extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: RequestUnstakeTokenInput<TAccountOwner, TAccountTokenStake>,
  config?: { programAddress?: TProgramAddress },
): RequestUnstakeTokenInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountTokenStake
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    tokenStake: { value: input.tokenStake ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("tokenStake", accounts.tokenStake),
    ],
    data: getRequestUnstakeTokenInstructionDataEncoder().encode(
      args as RequestUnstakeTokenInstructionDataArgs,
    ),
    programAddress,
  } as RequestUnstakeTokenInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountTokenStake
  >);
}

export type ParsedRequestUnstakeTokenInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    tokenStake: TAccountMetas[1];
  };
  data: RequestUnstakeTokenInstructionData;
};

export function parseRequestUnstakeTokenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestUnstakeTokenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), tokenStake: getNextAccount() },
    data: getRequestUnstakeTokenInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
