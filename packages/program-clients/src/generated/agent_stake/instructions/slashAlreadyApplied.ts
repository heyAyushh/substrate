/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findSlashMarkerPda } from "../pdas";
import { AGENT_STAKE_PROGRAM_ADDRESS } from "../programs";

export const SLASH_ALREADY_APPLIED_DISCRIMINATOR = new Uint8Array([
  111, 254, 113, 204, 61, 134, 160, 255,
]);

export function getSlashAlreadyAppliedDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SLASH_ALREADY_APPLIED_DISCRIMINATOR,
  );
}

export type SlashAlreadyAppliedInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountSlashAuthority extends string | AccountMeta<string> = string,
  TAccountStake extends string | AccountMeta<string> = string,
  TAccountDisputeReceipt extends string | AccountMeta<string> = string,
  TAccountSlashMarker extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSlashAuthority extends string
        ? ReadonlySignerAccount<TAccountSlashAuthority> &
            AccountSignerMeta<TAccountSlashAuthority>
        : TAccountSlashAuthority,
      TAccountStake extends string
        ? ReadonlyAccount<TAccountStake>
        : TAccountStake,
      TAccountDisputeReceipt extends string
        ? ReadonlyAccount<TAccountDisputeReceipt>
        : TAccountDisputeReceipt,
      TAccountSlashMarker extends string
        ? ReadonlyAccount<TAccountSlashMarker>
        : TAccountSlashMarker,
      ...TRemainingAccounts,
    ]
  >;

export type SlashAlreadyAppliedInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SlashAlreadyAppliedInstructionDataArgs = {};

export function getSlashAlreadyAppliedInstructionDataEncoder(): FixedSizeEncoder<SlashAlreadyAppliedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: SLASH_ALREADY_APPLIED_DISCRIMINATOR,
    }),
  );
}

export function getSlashAlreadyAppliedInstructionDataDecoder(): FixedSizeDecoder<SlashAlreadyAppliedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSlashAlreadyAppliedInstructionDataCodec(): FixedSizeCodec<
  SlashAlreadyAppliedInstructionDataArgs,
  SlashAlreadyAppliedInstructionData
> {
  return combineCodec(
    getSlashAlreadyAppliedInstructionDataEncoder(),
    getSlashAlreadyAppliedInstructionDataDecoder(),
  );
}

export type SlashAlreadyAppliedAsyncInput<
  TAccountSlashAuthority extends string = string,
  TAccountStake extends string = string,
  TAccountDisputeReceipt extends string = string,
  TAccountSlashMarker extends string = string,
> = {
  slashAuthority: TransactionSigner<TAccountSlashAuthority>;
  stake: Address<TAccountStake>;
  disputeReceipt: Address<TAccountDisputeReceipt>;
  slashMarker?: Address<TAccountSlashMarker>;
};

export async function getSlashAlreadyAppliedInstructionAsync<
  TAccountSlashAuthority extends string,
  TAccountStake extends string,
  TAccountDisputeReceipt extends string,
  TAccountSlashMarker extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: SlashAlreadyAppliedAsyncInput<
    TAccountSlashAuthority,
    TAccountStake,
    TAccountDisputeReceipt,
    TAccountSlashMarker
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SlashAlreadyAppliedInstruction<
    TProgramAddress,
    TAccountSlashAuthority,
    TAccountStake,
    TAccountDisputeReceipt,
    TAccountSlashMarker
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    slashAuthority: { value: input.slashAuthority ?? null, isWritable: false },
    stake: { value: input.stake ?? null, isWritable: false },
    disputeReceipt: { value: input.disputeReceipt ?? null, isWritable: false },
    slashMarker: { value: input.slashMarker ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.slashMarker.value) {
    accounts.slashMarker.value = await findSlashMarkerPda({
      stake: getAddressFromResolvedInstructionAccount(
        "stake",
        accounts.stake.value,
      ),
      disputeReceipt: getAddressFromResolvedInstructionAccount(
        "disputeReceipt",
        accounts.disputeReceipt.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("slashAuthority", accounts.slashAuthority),
      getAccountMeta("stake", accounts.stake),
      getAccountMeta("disputeReceipt", accounts.disputeReceipt),
      getAccountMeta("slashMarker", accounts.slashMarker),
    ],
    data: getSlashAlreadyAppliedInstructionDataEncoder().encode({}),
    programAddress,
  } as SlashAlreadyAppliedInstruction<
    TProgramAddress,
    TAccountSlashAuthority,
    TAccountStake,
    TAccountDisputeReceipt,
    TAccountSlashMarker
  >);
}

export type SlashAlreadyAppliedInput<
  TAccountSlashAuthority extends string = string,
  TAccountStake extends string = string,
  TAccountDisputeReceipt extends string = string,
  TAccountSlashMarker extends string = string,
> = {
  slashAuthority: TransactionSigner<TAccountSlashAuthority>;
  stake: Address<TAccountStake>;
  disputeReceipt: Address<TAccountDisputeReceipt>;
  slashMarker: Address<TAccountSlashMarker>;
};

export function getSlashAlreadyAppliedInstruction<
  TAccountSlashAuthority extends string,
  TAccountStake extends string,
  TAccountDisputeReceipt extends string,
  TAccountSlashMarker extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: SlashAlreadyAppliedInput<
    TAccountSlashAuthority,
    TAccountStake,
    TAccountDisputeReceipt,
    TAccountSlashMarker
  >,
  config?: { programAddress?: TProgramAddress },
): SlashAlreadyAppliedInstruction<
  TProgramAddress,
  TAccountSlashAuthority,
  TAccountStake,
  TAccountDisputeReceipt,
  TAccountSlashMarker
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    slashAuthority: { value: input.slashAuthority ?? null, isWritable: false },
    stake: { value: input.stake ?? null, isWritable: false },
    disputeReceipt: { value: input.disputeReceipt ?? null, isWritable: false },
    slashMarker: { value: input.slashMarker ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("slashAuthority", accounts.slashAuthority),
      getAccountMeta("stake", accounts.stake),
      getAccountMeta("disputeReceipt", accounts.disputeReceipt),
      getAccountMeta("slashMarker", accounts.slashMarker),
    ],
    data: getSlashAlreadyAppliedInstructionDataEncoder().encode({}),
    programAddress,
  } as SlashAlreadyAppliedInstruction<
    TProgramAddress,
    TAccountSlashAuthority,
    TAccountStake,
    TAccountDisputeReceipt,
    TAccountSlashMarker
  >);
}

export type ParsedSlashAlreadyAppliedInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    slashAuthority: TAccountMetas[0];
    stake: TAccountMetas[1];
    disputeReceipt: TAccountMetas[2];
    slashMarker: TAccountMetas[3];
  };
  data: SlashAlreadyAppliedInstructionData;
};

export function parseSlashAlreadyAppliedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSlashAlreadyAppliedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      slashAuthority: getNextAccount(),
      stake: getNextAccount(),
      disputeReceipt: getNextAccount(),
      slashMarker: getNextAccount(),
    },
    data: getSlashAlreadyAppliedInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
