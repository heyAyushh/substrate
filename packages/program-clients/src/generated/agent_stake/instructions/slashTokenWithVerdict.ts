/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findSlashMarkerPda, findTreasuryTokenVaultPda } from "../pdas";
import { AGENT_STAKE_PROGRAM_ADDRESS } from "../programs";

export const SLASH_TOKEN_WITH_VERDICT_DISCRIMINATOR = new Uint8Array([
  220, 204, 237, 82, 35, 134, 65, 22,
]);

export function getSlashTokenWithVerdictDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SLASH_TOKEN_WITH_VERDICT_DISCRIMINATOR,
  );
}

export type SlashTokenWithVerdictInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountAdjudicator extends string | AccountMeta<string> = string,
  TAccountTokenStake extends string | AccountMeta<string> = string,
  TAccountDisputeReceipt extends string | AccountMeta<string> = string,
  TAccountVerdict extends string | AccountMeta<string> = string,
  TAccountSlashMarker extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountTreasuryTokenVault extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdjudicator extends string
        ? WritableSignerAccount<TAccountAdjudicator> &
            AccountSignerMeta<TAccountAdjudicator>
        : TAccountAdjudicator,
      TAccountTokenStake extends string
        ? WritableAccount<TAccountTokenStake>
        : TAccountTokenStake,
      TAccountDisputeReceipt extends string
        ? ReadonlyAccount<TAccountDisputeReceipt>
        : TAccountDisputeReceipt,
      TAccountVerdict extends string
        ? ReadonlyAccount<TAccountVerdict>
        : TAccountVerdict,
      TAccountSlashMarker extends string
        ? WritableAccount<TAccountSlashMarker>
        : TAccountSlashMarker,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountTreasuryTokenVault extends string
        ? WritableAccount<TAccountTreasuryTokenVault>
        : TAccountTreasuryTokenVault,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SlashTokenWithVerdictInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SlashTokenWithVerdictInstructionDataArgs = {};

export function getSlashTokenWithVerdictInstructionDataEncoder(): FixedSizeEncoder<SlashTokenWithVerdictInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: SLASH_TOKEN_WITH_VERDICT_DISCRIMINATOR,
    }),
  );
}

export function getSlashTokenWithVerdictInstructionDataDecoder(): FixedSizeDecoder<SlashTokenWithVerdictInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSlashTokenWithVerdictInstructionDataCodec(): FixedSizeCodec<
  SlashTokenWithVerdictInstructionDataArgs,
  SlashTokenWithVerdictInstructionData
> {
  return combineCodec(
    getSlashTokenWithVerdictInstructionDataEncoder(),
    getSlashTokenWithVerdictInstructionDataDecoder(),
  );
}

export type SlashTokenWithVerdictAsyncInput<
  TAccountAdjudicator extends string = string,
  TAccountTokenStake extends string = string,
  TAccountDisputeReceipt extends string = string,
  TAccountVerdict extends string = string,
  TAccountSlashMarker extends string = string,
  TAccountMint extends string = string,
  TAccountVault extends string = string,
  TAccountTreasuryTokenVault extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  adjudicator: TransactionSigner<TAccountAdjudicator>;
  tokenStake: Address<TAccountTokenStake>;
  disputeReceipt: Address<TAccountDisputeReceipt>;
  verdict?: Address<TAccountVerdict>;
  slashMarker?: Address<TAccountSlashMarker>;
  mint: Address<TAccountMint>;
  /** and the SPL Token program validates it as a token account during transfer. */
  vault: Address<TAccountVault>;
  /** mint; the SPL Token program validates the account and mint on receipt. */
  treasuryTokenVault?: Address<TAccountTreasuryTokenVault>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getSlashTokenWithVerdictInstructionAsync<
  TAccountAdjudicator extends string,
  TAccountTokenStake extends string,
  TAccountDisputeReceipt extends string,
  TAccountVerdict extends string,
  TAccountSlashMarker extends string,
  TAccountMint extends string,
  TAccountVault extends string,
  TAccountTreasuryTokenVault extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: SlashTokenWithVerdictAsyncInput<
    TAccountAdjudicator,
    TAccountTokenStake,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSlashMarker,
    TAccountMint,
    TAccountVault,
    TAccountTreasuryTokenVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SlashTokenWithVerdictInstruction<
    TProgramAddress,
    TAccountAdjudicator,
    TAccountTokenStake,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSlashMarker,
    TAccountMint,
    TAccountVault,
    TAccountTreasuryTokenVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    adjudicator: { value: input.adjudicator ?? null, isWritable: true },
    tokenStake: { value: input.tokenStake ?? null, isWritable: true },
    disputeReceipt: { value: input.disputeReceipt ?? null, isWritable: false },
    verdict: { value: input.verdict ?? null, isWritable: false },
    slashMarker: { value: input.slashMarker ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    treasuryTokenVault: {
      value: input.treasuryTokenVault ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.verdict.value) {
    accounts.verdict.value = await getProgramDerivedAddress({
      programAddress:
        "uJx2R2MHL7PEob6UPNz2DevGKpwd35fnKCrDQoavbtF" as Address<"uJx2R2MHL7PEob6UPNz2DevGKpwd35fnKCrDQoavbtF">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([118, 101, 114, 100, 105, 99, 116]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "disputeReceipt",
            accounts.disputeReceipt.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.slashMarker.value) {
    accounts.slashMarker.value = await findSlashMarkerPda({
      tokenStake: getAddressFromResolvedInstructionAccount(
        "tokenStake",
        accounts.tokenStake.value,
      ),
      disputeReceipt: getAddressFromResolvedInstructionAccount(
        "disputeReceipt",
        accounts.disputeReceipt.value,
      ),
    });
  }
  if (!accounts.treasuryTokenVault.value) {
    accounts.treasuryTokenVault.value = await findTreasuryTokenVaultPda({
      mint: getAddressFromResolvedInstructionAccount(
        "mint",
        accounts.mint.value,
      ),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("adjudicator", accounts.adjudicator),
      getAccountMeta("tokenStake", accounts.tokenStake),
      getAccountMeta("disputeReceipt", accounts.disputeReceipt),
      getAccountMeta("verdict", accounts.verdict),
      getAccountMeta("slashMarker", accounts.slashMarker),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("treasuryTokenVault", accounts.treasuryTokenVault),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSlashTokenWithVerdictInstructionDataEncoder().encode({}),
    programAddress,
  } as SlashTokenWithVerdictInstruction<
    TProgramAddress,
    TAccountAdjudicator,
    TAccountTokenStake,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSlashMarker,
    TAccountMint,
    TAccountVault,
    TAccountTreasuryTokenVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type SlashTokenWithVerdictInput<
  TAccountAdjudicator extends string = string,
  TAccountTokenStake extends string = string,
  TAccountDisputeReceipt extends string = string,
  TAccountVerdict extends string = string,
  TAccountSlashMarker extends string = string,
  TAccountMint extends string = string,
  TAccountVault extends string = string,
  TAccountTreasuryTokenVault extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  adjudicator: TransactionSigner<TAccountAdjudicator>;
  tokenStake: Address<TAccountTokenStake>;
  disputeReceipt: Address<TAccountDisputeReceipt>;
  verdict: Address<TAccountVerdict>;
  slashMarker: Address<TAccountSlashMarker>;
  mint: Address<TAccountMint>;
  /** and the SPL Token program validates it as a token account during transfer. */
  vault: Address<TAccountVault>;
  /** mint; the SPL Token program validates the account and mint on receipt. */
  treasuryTokenVault: Address<TAccountTreasuryTokenVault>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getSlashTokenWithVerdictInstruction<
  TAccountAdjudicator extends string,
  TAccountTokenStake extends string,
  TAccountDisputeReceipt extends string,
  TAccountVerdict extends string,
  TAccountSlashMarker extends string,
  TAccountMint extends string,
  TAccountVault extends string,
  TAccountTreasuryTokenVault extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: SlashTokenWithVerdictInput<
    TAccountAdjudicator,
    TAccountTokenStake,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSlashMarker,
    TAccountMint,
    TAccountVault,
    TAccountTreasuryTokenVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SlashTokenWithVerdictInstruction<
  TProgramAddress,
  TAccountAdjudicator,
  TAccountTokenStake,
  TAccountDisputeReceipt,
  TAccountVerdict,
  TAccountSlashMarker,
  TAccountMint,
  TAccountVault,
  TAccountTreasuryTokenVault,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    adjudicator: { value: input.adjudicator ?? null, isWritable: true },
    tokenStake: { value: input.tokenStake ?? null, isWritable: true },
    disputeReceipt: { value: input.disputeReceipt ?? null, isWritable: false },
    verdict: { value: input.verdict ?? null, isWritable: false },
    slashMarker: { value: input.slashMarker ?? null, isWritable: true },
    mint: { value: input.mint ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    treasuryTokenVault: {
      value: input.treasuryTokenVault ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("adjudicator", accounts.adjudicator),
      getAccountMeta("tokenStake", accounts.tokenStake),
      getAccountMeta("disputeReceipt", accounts.disputeReceipt),
      getAccountMeta("verdict", accounts.verdict),
      getAccountMeta("slashMarker", accounts.slashMarker),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("treasuryTokenVault", accounts.treasuryTokenVault),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSlashTokenWithVerdictInstructionDataEncoder().encode({}),
    programAddress,
  } as SlashTokenWithVerdictInstruction<
    TProgramAddress,
    TAccountAdjudicator,
    TAccountTokenStake,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSlashMarker,
    TAccountMint,
    TAccountVault,
    TAccountTreasuryTokenVault,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedSlashTokenWithVerdictInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    adjudicator: TAccountMetas[0];
    tokenStake: TAccountMetas[1];
    disputeReceipt: TAccountMetas[2];
    verdict: TAccountMetas[3];
    slashMarker: TAccountMetas[4];
    mint: TAccountMetas[5];
    /** and the SPL Token program validates it as a token account during transfer. */
    vault: TAccountMetas[6];
    /** mint; the SPL Token program validates the account and mint on receipt. */
    treasuryTokenVault: TAccountMetas[7];
    tokenProgram: TAccountMetas[8];
    systemProgram: TAccountMetas[9];
  };
  data: SlashTokenWithVerdictInstructionData;
};

export function parseSlashTokenWithVerdictInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSlashTokenWithVerdictInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      adjudicator: getNextAccount(),
      tokenStake: getNextAccount(),
      disputeReceipt: getNextAccount(),
      verdict: getNextAccount(),
      slashMarker: getNextAccount(),
      mint: getNextAccount(),
      vault: getNextAccount(),
      treasuryTokenVault: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSlashTokenWithVerdictInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
