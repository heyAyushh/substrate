/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findSlashMarkerPda } from "../pdas";
import { AGENT_STAKE_PROGRAM_ADDRESS } from "../programs";

export const SLASH_WITH_VERDICT_DISCRIMINATOR = new Uint8Array([
  0, 73, 124, 47, 196, 243, 130, 71,
]);

export function getSlashWithVerdictDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SLASH_WITH_VERDICT_DISCRIMINATOR,
  );
}

export type SlashWithVerdictInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountAdjudicator extends string | AccountMeta<string> = string,
  TAccountStake extends string | AccountMeta<string> = string,
  TAccountDisputeReceipt extends string | AccountMeta<string> = string,
  TAccountVerdict extends string | AccountMeta<string> = string,
  TAccountSlashMarker extends string | AccountMeta<string> = string,
  TAccountTreasuryVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdjudicator extends string
        ? WritableSignerAccount<TAccountAdjudicator> &
            AccountSignerMeta<TAccountAdjudicator>
        : TAccountAdjudicator,
      TAccountStake extends string
        ? WritableAccount<TAccountStake>
        : TAccountStake,
      TAccountDisputeReceipt extends string
        ? ReadonlyAccount<TAccountDisputeReceipt>
        : TAccountDisputeReceipt,
      TAccountVerdict extends string
        ? ReadonlyAccount<TAccountVerdict>
        : TAccountVerdict,
      TAccountSlashMarker extends string
        ? WritableAccount<TAccountSlashMarker>
        : TAccountSlashMarker,
      TAccountTreasuryVault extends string
        ? WritableAccount<TAccountTreasuryVault>
        : TAccountTreasuryVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SlashWithVerdictInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SlashWithVerdictInstructionDataArgs = {};

export function getSlashWithVerdictInstructionDataEncoder(): FixedSizeEncoder<SlashWithVerdictInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SLASH_WITH_VERDICT_DISCRIMINATOR }),
  );
}

export function getSlashWithVerdictInstructionDataDecoder(): FixedSizeDecoder<SlashWithVerdictInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSlashWithVerdictInstructionDataCodec(): FixedSizeCodec<
  SlashWithVerdictInstructionDataArgs,
  SlashWithVerdictInstructionData
> {
  return combineCodec(
    getSlashWithVerdictInstructionDataEncoder(),
    getSlashWithVerdictInstructionDataDecoder(),
  );
}

export type SlashWithVerdictAsyncInput<
  TAccountAdjudicator extends string = string,
  TAccountStake extends string = string,
  TAccountDisputeReceipt extends string = string,
  TAccountVerdict extends string = string,
  TAccountSlashMarker extends string = string,
  TAccountTreasuryVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  adjudicator: TransactionSigner<TAccountAdjudicator>;
  stake: Address<TAccountStake>;
  disputeReceipt: Address<TAccountDisputeReceipt>;
  verdict?: Address<TAccountVerdict>;
  slashMarker?: Address<TAccountSlashMarker>;
  treasuryVault?: Address<TAccountTreasuryVault>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getSlashWithVerdictInstructionAsync<
  TAccountAdjudicator extends string,
  TAccountStake extends string,
  TAccountDisputeReceipt extends string,
  TAccountVerdict extends string,
  TAccountSlashMarker extends string,
  TAccountTreasuryVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: SlashWithVerdictAsyncInput<
    TAccountAdjudicator,
    TAccountStake,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSlashMarker,
    TAccountTreasuryVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SlashWithVerdictInstruction<
    TProgramAddress,
    TAccountAdjudicator,
    TAccountStake,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSlashMarker,
    TAccountTreasuryVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    adjudicator: { value: input.adjudicator ?? null, isWritable: true },
    stake: { value: input.stake ?? null, isWritable: true },
    disputeReceipt: { value: input.disputeReceipt ?? null, isWritable: false },
    verdict: { value: input.verdict ?? null, isWritable: false },
    slashMarker: { value: input.slashMarker ?? null, isWritable: true },
    treasuryVault: { value: input.treasuryVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.verdict.value) {
    accounts.verdict.value = await getProgramDerivedAddress({
      programAddress:
        "9cYSvQHM78shtFPnpxSfHwyB26CArahmHuJt7byyUrHa" as Address<"9cYSvQHM78shtFPnpxSfHwyB26CArahmHuJt7byyUrHa">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([118, 101, 114, 100, 105, 99, 116]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "disputeReceipt",
            accounts.disputeReceipt.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.slashMarker.value) {
    accounts.slashMarker.value = await findSlashMarkerPda({
      stake: getAddressFromResolvedInstructionAccount(
        "stake",
        accounts.stake.value,
      ),
      disputeReceipt: getAddressFromResolvedInstructionAccount(
        "disputeReceipt",
        accounts.disputeReceipt.value,
      ),
    });
  }
  if (!accounts.treasuryVault.value) {
    accounts.treasuryVault.value = await getProgramDerivedAddress({
      programAddress:
        "9cYSvQHM78shtFPnpxSfHwyB26CArahmHuJt7byyUrHa" as Address<"9cYSvQHM78shtFPnpxSfHwyB26CArahmHuJt7byyUrHa">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([116, 114, 101, 97, 115, 117, 114, 121]),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("adjudicator", accounts.adjudicator),
      getAccountMeta("stake", accounts.stake),
      getAccountMeta("disputeReceipt", accounts.disputeReceipt),
      getAccountMeta("verdict", accounts.verdict),
      getAccountMeta("slashMarker", accounts.slashMarker),
      getAccountMeta("treasuryVault", accounts.treasuryVault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSlashWithVerdictInstructionDataEncoder().encode({}),
    programAddress,
  } as SlashWithVerdictInstruction<
    TProgramAddress,
    TAccountAdjudicator,
    TAccountStake,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSlashMarker,
    TAccountTreasuryVault,
    TAccountSystemProgram
  >);
}

export type SlashWithVerdictInput<
  TAccountAdjudicator extends string = string,
  TAccountStake extends string = string,
  TAccountDisputeReceipt extends string = string,
  TAccountVerdict extends string = string,
  TAccountSlashMarker extends string = string,
  TAccountTreasuryVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  adjudicator: TransactionSigner<TAccountAdjudicator>;
  stake: Address<TAccountStake>;
  disputeReceipt: Address<TAccountDisputeReceipt>;
  verdict: Address<TAccountVerdict>;
  slashMarker: Address<TAccountSlashMarker>;
  treasuryVault: Address<TAccountTreasuryVault>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getSlashWithVerdictInstruction<
  TAccountAdjudicator extends string,
  TAccountStake extends string,
  TAccountDisputeReceipt extends string,
  TAccountVerdict extends string,
  TAccountSlashMarker extends string,
  TAccountTreasuryVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENT_STAKE_PROGRAM_ADDRESS,
>(
  input: SlashWithVerdictInput<
    TAccountAdjudicator,
    TAccountStake,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSlashMarker,
    TAccountTreasuryVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SlashWithVerdictInstruction<
  TProgramAddress,
  TAccountAdjudicator,
  TAccountStake,
  TAccountDisputeReceipt,
  TAccountVerdict,
  TAccountSlashMarker,
  TAccountTreasuryVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENT_STAKE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    adjudicator: { value: input.adjudicator ?? null, isWritable: true },
    stake: { value: input.stake ?? null, isWritable: true },
    disputeReceipt: { value: input.disputeReceipt ?? null, isWritable: false },
    verdict: { value: input.verdict ?? null, isWritable: false },
    slashMarker: { value: input.slashMarker ?? null, isWritable: true },
    treasuryVault: { value: input.treasuryVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("adjudicator", accounts.adjudicator),
      getAccountMeta("stake", accounts.stake),
      getAccountMeta("disputeReceipt", accounts.disputeReceipt),
      getAccountMeta("verdict", accounts.verdict),
      getAccountMeta("slashMarker", accounts.slashMarker),
      getAccountMeta("treasuryVault", accounts.treasuryVault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSlashWithVerdictInstructionDataEncoder().encode({}),
    programAddress,
  } as SlashWithVerdictInstruction<
    TProgramAddress,
    TAccountAdjudicator,
    TAccountStake,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSlashMarker,
    TAccountTreasuryVault,
    TAccountSystemProgram
  >);
}

export type ParsedSlashWithVerdictInstruction<
  TProgram extends string = typeof AGENT_STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    adjudicator: TAccountMetas[0];
    stake: TAccountMetas[1];
    disputeReceipt: TAccountMetas[2];
    verdict: TAccountMetas[3];
    slashMarker: TAccountMetas[4];
    treasuryVault: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: SlashWithVerdictInstructionData;
};

export function parseSlashWithVerdictInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSlashWithVerdictInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      adjudicator: getNextAccount(),
      stake: getNextAccount(),
      disputeReceipt: getNextAccount(),
      verdict: getNextAccount(),
      slashMarker: getNextAccount(),
      treasuryVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSlashWithVerdictInstructionDataDecoder().decode(instruction.data),
  };
}
