/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { ATTESTER_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_ATTESTER_DISCRIMINATOR = new Uint8Array([
  46, 172, 55, 137, 201, 217, 10, 143,
]);

export function getCloseAttesterDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_ATTESTER_DISCRIMINATOR,
  );
}

export type CloseAttesterInstruction<
  TProgram extends string = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAttester extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAttester extends string
        ? WritableAccount<TAccountAttester>
        : TAccountAttester,
      ...TRemainingAccounts,
    ]
  >;

export type CloseAttesterInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseAttesterInstructionDataArgs = {};

export function getCloseAttesterInstructionDataEncoder(): FixedSizeEncoder<CloseAttesterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_ATTESTER_DISCRIMINATOR }),
  );
}

export function getCloseAttesterInstructionDataDecoder(): FixedSizeDecoder<CloseAttesterInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseAttesterInstructionDataCodec(): FixedSizeCodec<
  CloseAttesterInstructionDataArgs,
  CloseAttesterInstructionData
> {
  return combineCodec(
    getCloseAttesterInstructionDataEncoder(),
    getCloseAttesterInstructionDataDecoder(),
  );
}

export type CloseAttesterInput<
  TAccountAuthority extends string = string,
  TAccountAttester extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  attester: Address<TAccountAttester>;
};

export function getCloseAttesterInstruction<
  TAccountAuthority extends string,
  TAccountAttester extends string,
  TProgramAddress extends Address = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CloseAttesterInput<TAccountAuthority, TAccountAttester>,
  config?: { programAddress?: TProgramAddress },
): CloseAttesterInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAttester
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ATTESTER_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    attester: { value: input.attester ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("attester", accounts.attester),
    ],
    data: getCloseAttesterInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseAttesterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAttester
  >);
}

export type ParsedCloseAttesterInstruction<
  TProgram extends string = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    attester: TAccountMetas[1];
  };
  data: CloseAttesterInstructionData;
};

export function parseCloseAttesterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseAttesterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), attester: getNextAccount() },
    data: getCloseAttesterInstructionDataDecoder().decode(instruction.data),
  };
}
