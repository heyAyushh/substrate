/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findAttesterPda, findConfigPda } from "../pdas";
import { ATTESTER_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const REGISTER_ATTESTER_DISCRIMINATOR = new Uint8Array([
  111, 233, 124, 30, 66, 228, 125, 85,
]);

export function getRegisterAttesterDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_ATTESTER_DISCRIMINATOR,
  );
}

export type RegisterAttesterInstruction<
  TProgram extends string = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountIdentityBond extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAttester extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? ReadonlyAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountIdentityBond extends string
        ? ReadonlyAccount<TAccountIdentityBond>
        : TAccountIdentityBond,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAttester extends string
        ? WritableAccount<TAccountAttester>
        : TAccountAttester,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterAttesterInstructionData = {
  discriminator: ReadonlyUint8Array;
  category: string;
  selfDeclaredTier: number;
};

export type RegisterAttesterInstructionDataArgs = {
  category: string;
  selfDeclaredTier: number;
};

export function getRegisterAttesterInstructionDataEncoder(): Encoder<RegisterAttesterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["category", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["selfDeclaredTier", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_ATTESTER_DISCRIMINATOR }),
  );
}

export function getRegisterAttesterInstructionDataDecoder(): Decoder<RegisterAttesterInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["category", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["selfDeclaredTier", getU8Decoder()],
  ]);
}

export function getRegisterAttesterInstructionDataCodec(): Codec<
  RegisterAttesterInstructionDataArgs,
  RegisterAttesterInstructionData
> {
  return combineCodec(
    getRegisterAttesterInstructionDataEncoder(),
    getRegisterAttesterInstructionDataDecoder(),
  );
}

export type RegisterAttesterAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountIdentityBond extends string = string,
  TAccountConfig extends string = string,
  TAccountAttester extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  identityBond: Address<TAccountIdentityBond>;
  config?: Address<TAccountConfig>;
  attester?: Address<TAccountAttester>;
  systemProgram?: Address<TAccountSystemProgram>;
  category: RegisterAttesterInstructionDataArgs["category"];
  selfDeclaredTier: RegisterAttesterInstructionDataArgs["selfDeclaredTier"];
};

export async function getRegisterAttesterInstructionAsync<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountIdentityBond extends string,
  TAccountConfig extends string,
  TAccountAttester extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RegisterAttesterAsyncInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond,
    TAccountConfig,
    TAccountAttester,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterAttesterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond,
    TAccountConfig,
    TAccountAttester,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ATTESTER_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    identityBond: { value: input.identityBond ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    attester: { value: input.attester ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.attester.value) {
    accounts.attester.value = await findAttesterPda({
      identity: getAddressFromResolvedInstructionAccount(
        "identity",
        accounts.identity.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("identityBond", accounts.identityBond),
      getAccountMeta("config", accounts.config),
      getAccountMeta("attester", accounts.attester),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterAttesterInstructionDataEncoder().encode(
      args as RegisterAttesterInstructionDataArgs,
    ),
    programAddress,
  } as RegisterAttesterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond,
    TAccountConfig,
    TAccountAttester,
    TAccountSystemProgram
  >);
}

export type RegisterAttesterInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountIdentityBond extends string = string,
  TAccountConfig extends string = string,
  TAccountAttester extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  identityBond: Address<TAccountIdentityBond>;
  config: Address<TAccountConfig>;
  attester: Address<TAccountAttester>;
  systemProgram?: Address<TAccountSystemProgram>;
  category: RegisterAttesterInstructionDataArgs["category"];
  selfDeclaredTier: RegisterAttesterInstructionDataArgs["selfDeclaredTier"];
};

export function getRegisterAttesterInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountIdentityBond extends string,
  TAccountConfig extends string,
  TAccountAttester extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
>(
  input: RegisterAttesterInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond,
    TAccountConfig,
    TAccountAttester,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterAttesterInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity,
  TAccountIdentityBond,
  TAccountConfig,
  TAccountAttester,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ATTESTER_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    identityBond: { value: input.identityBond ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    attester: { value: input.attester ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("identityBond", accounts.identityBond),
      getAccountMeta("config", accounts.config),
      getAccountMeta("attester", accounts.attester),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterAttesterInstructionDataEncoder().encode(
      args as RegisterAttesterInstructionDataArgs,
    ),
    programAddress,
  } as RegisterAttesterInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond,
    TAccountConfig,
    TAccountAttester,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterAttesterInstruction<
  TProgram extends string = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
    identityBond: TAccountMetas[2];
    config: TAccountMetas[3];
    attester: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: RegisterAttesterInstructionData;
};

export function parseRegisterAttesterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterAttesterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      identity: getNextAccount(),
      identityBond: getNextAccount(),
      config: getNextAccount(),
      attester: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterAttesterInstructionDataDecoder().decode(instruction.data),
  };
}
