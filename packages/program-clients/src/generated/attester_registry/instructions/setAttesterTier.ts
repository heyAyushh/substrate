/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas";
import { ATTESTER_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const SET_ATTESTER_TIER_DISCRIMINATOR = new Uint8Array([
  218, 177, 31, 39, 61, 122, 71, 233,
]);

export function getSetAttesterTierDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_ATTESTER_TIER_DISCRIMINATOR,
  );
}

export type SetAttesterTierInstruction<
  TProgram extends string = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
  TAccountCurator extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAttester extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCurator extends string
        ? WritableSignerAccount<TAccountCurator> &
            AccountSignerMeta<TAccountCurator>
        : TAccountCurator,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAttester extends string
        ? WritableAccount<TAccountAttester>
        : TAccountAttester,
      ...TRemainingAccounts,
    ]
  >;

export type SetAttesterTierInstructionData = {
  discriminator: ReadonlyUint8Array;
  effectiveTier: number;
};

export type SetAttesterTierInstructionDataArgs = { effectiveTier: number };

export function getSetAttesterTierInstructionDataEncoder(): FixedSizeEncoder<SetAttesterTierInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["effectiveTier", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_ATTESTER_TIER_DISCRIMINATOR }),
  );
}

export function getSetAttesterTierInstructionDataDecoder(): FixedSizeDecoder<SetAttesterTierInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["effectiveTier", getU8Decoder()],
  ]);
}

export function getSetAttesterTierInstructionDataCodec(): FixedSizeCodec<
  SetAttesterTierInstructionDataArgs,
  SetAttesterTierInstructionData
> {
  return combineCodec(
    getSetAttesterTierInstructionDataEncoder(),
    getSetAttesterTierInstructionDataDecoder(),
  );
}

export type SetAttesterTierAsyncInput<
  TAccountCurator extends string = string,
  TAccountConfig extends string = string,
  TAccountAttester extends string = string,
> = {
  curator: TransactionSigner<TAccountCurator>;
  config?: Address<TAccountConfig>;
  attester: Address<TAccountAttester>;
  effectiveTier: SetAttesterTierInstructionDataArgs["effectiveTier"];
};

export async function getSetAttesterTierInstructionAsync<
  TAccountCurator extends string,
  TAccountConfig extends string,
  TAccountAttester extends string,
  TProgramAddress extends Address = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetAttesterTierAsyncInput<
    TAccountCurator,
    TAccountConfig,
    TAccountAttester
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetAttesterTierInstruction<
    TProgramAddress,
    TAccountCurator,
    TAccountConfig,
    TAccountAttester
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ATTESTER_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    curator: { value: input.curator ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    attester: { value: input.attester ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("curator", accounts.curator),
      getAccountMeta("config", accounts.config),
      getAccountMeta("attester", accounts.attester),
    ],
    data: getSetAttesterTierInstructionDataEncoder().encode(
      args as SetAttesterTierInstructionDataArgs,
    ),
    programAddress,
  } as SetAttesterTierInstruction<
    TProgramAddress,
    TAccountCurator,
    TAccountConfig,
    TAccountAttester
  >);
}

export type SetAttesterTierInput<
  TAccountCurator extends string = string,
  TAccountConfig extends string = string,
  TAccountAttester extends string = string,
> = {
  curator: TransactionSigner<TAccountCurator>;
  config: Address<TAccountConfig>;
  attester: Address<TAccountAttester>;
  effectiveTier: SetAttesterTierInstructionDataArgs["effectiveTier"];
};

export function getSetAttesterTierInstruction<
  TAccountCurator extends string,
  TAccountConfig extends string,
  TAccountAttester extends string,
  TProgramAddress extends Address = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SetAttesterTierInput<
    TAccountCurator,
    TAccountConfig,
    TAccountAttester
  >,
  config?: { programAddress?: TProgramAddress },
): SetAttesterTierInstruction<
  TProgramAddress,
  TAccountCurator,
  TAccountConfig,
  TAccountAttester
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ATTESTER_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    curator: { value: input.curator ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    attester: { value: input.attester ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("curator", accounts.curator),
      getAccountMeta("config", accounts.config),
      getAccountMeta("attester", accounts.attester),
    ],
    data: getSetAttesterTierInstructionDataEncoder().encode(
      args as SetAttesterTierInstructionDataArgs,
    ),
    programAddress,
  } as SetAttesterTierInstruction<
    TProgramAddress,
    TAccountCurator,
    TAccountConfig,
    TAccountAttester
  >);
}

export type ParsedSetAttesterTierInstruction<
  TProgram extends string = typeof ATTESTER_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    curator: TAccountMetas[0];
    config: TAccountMetas[1];
    attester: TAccountMetas[2];
  };
  data: SetAttesterTierInstructionData;
};

export function parseSetAttesterTierInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetAttesterTierInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      curator: getNextAccount(),
      config: getNextAccount(),
      attester: getNextAccount(),
    },
    data: getSetAttesterTierInstructionDataDecoder().decode(instruction.data),
  };
}
