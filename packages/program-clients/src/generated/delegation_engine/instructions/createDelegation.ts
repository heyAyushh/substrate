/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findDelegationPda } from "../pdas";
import { DELEGATION_ENGINE_PROGRAM_ADDRESS } from "../programs";

export const CREATE_DELEGATION_DISCRIMINATOR = new Uint8Array([
  177, 165, 93, 55, 227, 163, 61, 175,
]);

export function getCreateDelegationDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_DELEGATION_DISCRIMINATOR,
  );
}

export type CreateDelegationInstruction<
  TProgram extends string = typeof DELEGATION_ENGINE_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountDelegate extends string | AccountMeta<string> = string,
  TAccountDelegation extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? ReadonlyAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountDelegate extends string
        ? ReadonlyAccount<TAccountDelegate>
        : TAccountDelegate,
      TAccountDelegation extends string
        ? WritableAccount<TAccountDelegation>
        : TAccountDelegation,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateDelegationInstructionData = {
  discriminator: ReadonlyUint8Array;
  allowedActions: number;
  expiresAtSlot: bigint;
};

export type CreateDelegationInstructionDataArgs = {
  allowedActions: number;
  expiresAtSlot: number | bigint;
};

export function getCreateDelegationInstructionDataEncoder(): FixedSizeEncoder<CreateDelegationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["allowedActions", getU8Encoder()],
      ["expiresAtSlot", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_DELEGATION_DISCRIMINATOR }),
  );
}

export function getCreateDelegationInstructionDataDecoder(): FixedSizeDecoder<CreateDelegationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["allowedActions", getU8Decoder()],
    ["expiresAtSlot", getU64Decoder()],
  ]);
}

export function getCreateDelegationInstructionDataCodec(): FixedSizeCodec<
  CreateDelegationInstructionDataArgs,
  CreateDelegationInstructionData
> {
  return combineCodec(
    getCreateDelegationInstructionDataEncoder(),
    getCreateDelegationInstructionDataDecoder(),
  );
}

export type CreateDelegationAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountDelegate extends string = string,
  TAccountDelegation extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  delegate: Address<TAccountDelegate>;
  delegation?: Address<TAccountDelegation>;
  systemProgram?: Address<TAccountSystemProgram>;
  allowedActions: CreateDelegationInstructionDataArgs["allowedActions"];
  expiresAtSlot: CreateDelegationInstructionDataArgs["expiresAtSlot"];
};

export async function getCreateDelegationInstructionAsync<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountDelegate extends string,
  TAccountDelegation extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DELEGATION_ENGINE_PROGRAM_ADDRESS,
>(
  input: CreateDelegationAsyncInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountDelegate,
    TAccountDelegation,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateDelegationInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountDelegate,
    TAccountDelegation,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? DELEGATION_ENGINE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    delegate: { value: input.delegate ?? null, isWritable: false },
    delegation: { value: input.delegation ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.delegation.value) {
    accounts.delegation.value = await findDelegationPda({
      identity: getAddressFromResolvedInstructionAccount(
        "identity",
        accounts.identity.value,
      ),
      delegate: getAddressFromResolvedInstructionAccount(
        "delegate",
        accounts.delegate.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("delegate", accounts.delegate),
      getAccountMeta("delegation", accounts.delegation),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateDelegationInstructionDataEncoder().encode(
      args as CreateDelegationInstructionDataArgs,
    ),
    programAddress,
  } as CreateDelegationInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountDelegate,
    TAccountDelegation,
    TAccountSystemProgram
  >);
}

export type CreateDelegationInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountDelegate extends string = string,
  TAccountDelegation extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  delegate: Address<TAccountDelegate>;
  delegation: Address<TAccountDelegation>;
  systemProgram?: Address<TAccountSystemProgram>;
  allowedActions: CreateDelegationInstructionDataArgs["allowedActions"];
  expiresAtSlot: CreateDelegationInstructionDataArgs["expiresAtSlot"];
};

export function getCreateDelegationInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountDelegate extends string,
  TAccountDelegation extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DELEGATION_ENGINE_PROGRAM_ADDRESS,
>(
  input: CreateDelegationInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountDelegate,
    TAccountDelegation,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateDelegationInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity,
  TAccountDelegate,
  TAccountDelegation,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? DELEGATION_ENGINE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    delegate: { value: input.delegate ?? null, isWritable: false },
    delegation: { value: input.delegation ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("delegate", accounts.delegate),
      getAccountMeta("delegation", accounts.delegation),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateDelegationInstructionDataEncoder().encode(
      args as CreateDelegationInstructionDataArgs,
    ),
    programAddress,
  } as CreateDelegationInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountDelegate,
    TAccountDelegation,
    TAccountSystemProgram
  >);
}

export type ParsedCreateDelegationInstruction<
  TProgram extends string = typeof DELEGATION_ENGINE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
    delegate: TAccountMetas[2];
    delegation: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateDelegationInstructionData;
};

export function parseCreateDelegationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateDelegationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      identity: getNextAccount(),
      delegate: getNextAccount(),
      delegation: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateDelegationInstructionDataDecoder().decode(instruction.data),
  };
}
