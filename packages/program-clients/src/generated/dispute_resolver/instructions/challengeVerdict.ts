/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { DISPUTE_RESOLVER_PROGRAM_ADDRESS } from "../programs";

export const CHALLENGE_VERDICT_DISCRIMINATOR = new Uint8Array([
  76, 170, 140, 175, 51, 98, 238, 230,
]);

export function getChallengeVerdictDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CHALLENGE_VERDICT_DISCRIMINATOR,
  );
}

export type ChallengeVerdictInstruction<
  TProgram extends string = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
  TAccountChallenger extends string | AccountMeta<string> = string,
  TAccountVerdict extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountChallenger extends string
        ? ReadonlySignerAccount<TAccountChallenger> &
            AccountSignerMeta<TAccountChallenger>
        : TAccountChallenger,
      TAccountVerdict extends string
        ? WritableAccount<TAccountVerdict>
        : TAccountVerdict,
      ...TRemainingAccounts,
    ]
  >;

export type ChallengeVerdictInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ChallengeVerdictInstructionDataArgs = {};

export function getChallengeVerdictInstructionDataEncoder(): FixedSizeEncoder<ChallengeVerdictInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CHALLENGE_VERDICT_DISCRIMINATOR }),
  );
}

export function getChallengeVerdictInstructionDataDecoder(): FixedSizeDecoder<ChallengeVerdictInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getChallengeVerdictInstructionDataCodec(): FixedSizeCodec<
  ChallengeVerdictInstructionDataArgs,
  ChallengeVerdictInstructionData
> {
  return combineCodec(
    getChallengeVerdictInstructionDataEncoder(),
    getChallengeVerdictInstructionDataDecoder(),
  );
}

export type ChallengeVerdictInput<
  TAccountChallenger extends string = string,
  TAccountVerdict extends string = string,
> = {
  challenger: TransactionSigner<TAccountChallenger>;
  verdict: Address<TAccountVerdict>;
};

export function getChallengeVerdictInstruction<
  TAccountChallenger extends string,
  TAccountVerdict extends string,
  TProgramAddress extends Address = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
>(
  input: ChallengeVerdictInput<TAccountChallenger, TAccountVerdict>,
  config?: { programAddress?: TProgramAddress },
): ChallengeVerdictInstruction<
  TProgramAddress,
  TAccountChallenger,
  TAccountVerdict
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? DISPUTE_RESOLVER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    challenger: { value: input.challenger ?? null, isWritable: false },
    verdict: { value: input.verdict ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("challenger", accounts.challenger),
      getAccountMeta("verdict", accounts.verdict),
    ],
    data: getChallengeVerdictInstructionDataEncoder().encode({}),
    programAddress,
  } as ChallengeVerdictInstruction<
    TProgramAddress,
    TAccountChallenger,
    TAccountVerdict
  >);
}

export type ParsedChallengeVerdictInstruction<
  TProgram extends string = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    challenger: TAccountMetas[0];
    verdict: TAccountMetas[1];
  };
  data: ChallengeVerdictInstructionData;
};

export function parseChallengeVerdictInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedChallengeVerdictInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { challenger: getNextAccount(), verdict: getNextAccount() },
    data: getChallengeVerdictInstructionDataDecoder().decode(instruction.data),
  };
}
