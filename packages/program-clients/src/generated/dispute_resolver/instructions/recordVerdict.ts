/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findAdjudicatorConfigPda, findVerdictPda } from "../pdas";
import { DISPUTE_RESOLVER_PROGRAM_ADDRESS } from "../programs";

export const RECORD_VERDICT_DISCRIMINATOR = new Uint8Array([
  251, 187, 180, 218, 5, 165, 217, 150,
]);

export function getRecordVerdictDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECORD_VERDICT_DISCRIMINATOR,
  );
}

export type RecordVerdictInstruction<
  TProgram extends string = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
  TAccountAdjudicator extends string | AccountMeta<string> = string,
  TAccountAdjudicatorConfig extends string | AccountMeta<string> = string,
  TAccountDisputeReceipt extends string | AccountMeta<string> = string,
  TAccountVerdict extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdjudicator extends string
        ? WritableSignerAccount<TAccountAdjudicator> &
            AccountSignerMeta<TAccountAdjudicator>
        : TAccountAdjudicator,
      TAccountAdjudicatorConfig extends string
        ? ReadonlyAccount<TAccountAdjudicatorConfig>
        : TAccountAdjudicatorConfig,
      TAccountDisputeReceipt extends string
        ? ReadonlyAccount<TAccountDisputeReceipt>
        : TAccountDisputeReceipt,
      TAccountVerdict extends string
        ? WritableAccount<TAccountVerdict>
        : TAccountVerdict,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RecordVerdictInstructionData = {
  discriminator: ReadonlyUint8Array;
  outcome: number;
  slashAmount: bigint;
  class: number;
  staleAfterSlot: bigint;
};

export type RecordVerdictInstructionDataArgs = {
  outcome: number;
  slashAmount: number | bigint;
  class: number;
  staleAfterSlot: number | bigint;
};

export function getRecordVerdictInstructionDataEncoder(): FixedSizeEncoder<RecordVerdictInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["outcome", getU8Encoder()],
      ["slashAmount", getU64Encoder()],
      ["class", getU8Encoder()],
      ["staleAfterSlot", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: RECORD_VERDICT_DISCRIMINATOR }),
  );
}

export function getRecordVerdictInstructionDataDecoder(): FixedSizeDecoder<RecordVerdictInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["outcome", getU8Decoder()],
    ["slashAmount", getU64Decoder()],
    ["class", getU8Decoder()],
    ["staleAfterSlot", getU64Decoder()],
  ]);
}

export function getRecordVerdictInstructionDataCodec(): FixedSizeCodec<
  RecordVerdictInstructionDataArgs,
  RecordVerdictInstructionData
> {
  return combineCodec(
    getRecordVerdictInstructionDataEncoder(),
    getRecordVerdictInstructionDataDecoder(),
  );
}

export type RecordVerdictAsyncInput<
  TAccountAdjudicator extends string = string,
  TAccountAdjudicatorConfig extends string = string,
  TAccountDisputeReceipt extends string = string,
  TAccountVerdict extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  adjudicator: TransactionSigner<TAccountAdjudicator>;
  adjudicatorConfig?: Address<TAccountAdjudicatorConfig>;
  disputeReceipt: Address<TAccountDisputeReceipt>;
  verdict?: Address<TAccountVerdict>;
  systemProgram?: Address<TAccountSystemProgram>;
  outcome: RecordVerdictInstructionDataArgs["outcome"];
  slashAmount: RecordVerdictInstructionDataArgs["slashAmount"];
  class: RecordVerdictInstructionDataArgs["class"];
  staleAfterSlot: RecordVerdictInstructionDataArgs["staleAfterSlot"];
};

export async function getRecordVerdictInstructionAsync<
  TAccountAdjudicator extends string,
  TAccountAdjudicatorConfig extends string,
  TAccountDisputeReceipt extends string,
  TAccountVerdict extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
>(
  input: RecordVerdictAsyncInput<
    TAccountAdjudicator,
    TAccountAdjudicatorConfig,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RecordVerdictInstruction<
    TProgramAddress,
    TAccountAdjudicator,
    TAccountAdjudicatorConfig,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? DISPUTE_RESOLVER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    adjudicator: { value: input.adjudicator ?? null, isWritable: true },
    adjudicatorConfig: {
      value: input.adjudicatorConfig ?? null,
      isWritable: false,
    },
    disputeReceipt: { value: input.disputeReceipt ?? null, isWritable: false },
    verdict: { value: input.verdict ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adjudicatorConfig.value) {
    accounts.adjudicatorConfig.value = await findAdjudicatorConfigPda();
  }
  if (!accounts.verdict.value) {
    accounts.verdict.value = await findVerdictPda({
      disputeReceipt: getAddressFromResolvedInstructionAccount(
        "disputeReceipt",
        accounts.disputeReceipt.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("adjudicator", accounts.adjudicator),
      getAccountMeta("adjudicatorConfig", accounts.adjudicatorConfig),
      getAccountMeta("disputeReceipt", accounts.disputeReceipt),
      getAccountMeta("verdict", accounts.verdict),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecordVerdictInstructionDataEncoder().encode(
      args as RecordVerdictInstructionDataArgs,
    ),
    programAddress,
  } as RecordVerdictInstruction<
    TProgramAddress,
    TAccountAdjudicator,
    TAccountAdjudicatorConfig,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSystemProgram
  >);
}

export type RecordVerdictInput<
  TAccountAdjudicator extends string = string,
  TAccountAdjudicatorConfig extends string = string,
  TAccountDisputeReceipt extends string = string,
  TAccountVerdict extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  adjudicator: TransactionSigner<TAccountAdjudicator>;
  adjudicatorConfig: Address<TAccountAdjudicatorConfig>;
  disputeReceipt: Address<TAccountDisputeReceipt>;
  verdict: Address<TAccountVerdict>;
  systemProgram?: Address<TAccountSystemProgram>;
  outcome: RecordVerdictInstructionDataArgs["outcome"];
  slashAmount: RecordVerdictInstructionDataArgs["slashAmount"];
  class: RecordVerdictInstructionDataArgs["class"];
  staleAfterSlot: RecordVerdictInstructionDataArgs["staleAfterSlot"];
};

export function getRecordVerdictInstruction<
  TAccountAdjudicator extends string,
  TAccountAdjudicatorConfig extends string,
  TAccountDisputeReceipt extends string,
  TAccountVerdict extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
>(
  input: RecordVerdictInput<
    TAccountAdjudicator,
    TAccountAdjudicatorConfig,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RecordVerdictInstruction<
  TProgramAddress,
  TAccountAdjudicator,
  TAccountAdjudicatorConfig,
  TAccountDisputeReceipt,
  TAccountVerdict,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? DISPUTE_RESOLVER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    adjudicator: { value: input.adjudicator ?? null, isWritable: true },
    adjudicatorConfig: {
      value: input.adjudicatorConfig ?? null,
      isWritable: false,
    },
    disputeReceipt: { value: input.disputeReceipt ?? null, isWritable: false },
    verdict: { value: input.verdict ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("adjudicator", accounts.adjudicator),
      getAccountMeta("adjudicatorConfig", accounts.adjudicatorConfig),
      getAccountMeta("disputeReceipt", accounts.disputeReceipt),
      getAccountMeta("verdict", accounts.verdict),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecordVerdictInstructionDataEncoder().encode(
      args as RecordVerdictInstructionDataArgs,
    ),
    programAddress,
  } as RecordVerdictInstruction<
    TProgramAddress,
    TAccountAdjudicator,
    TAccountAdjudicatorConfig,
    TAccountDisputeReceipt,
    TAccountVerdict,
    TAccountSystemProgram
  >);
}

export type ParsedRecordVerdictInstruction<
  TProgram extends string = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    adjudicator: TAccountMetas[0];
    adjudicatorConfig: TAccountMetas[1];
    disputeReceipt: TAccountMetas[2];
    verdict: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RecordVerdictInstructionData;
};

export function parseRecordVerdictInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecordVerdictInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      adjudicator: getNextAccount(),
      adjudicatorConfig: getNextAccount(),
      disputeReceipt: getNextAccount(),
      verdict: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRecordVerdictInstructionDataDecoder().decode(instruction.data),
  };
}
