/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findAdjudicatorConfigPda, findTreasuryVaultPda } from "../pdas";
import { DISPUTE_RESOLVER_PROGRAM_ADDRESS } from "../programs";

export const REGISTER_ADJUDICATOR_DISCRIMINATOR = new Uint8Array([
  112, 204, 98, 86, 215, 36, 55, 192,
]);

export function getRegisterAdjudicatorDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_ADJUDICATOR_DISCRIMINATOR,
  );
}

export type RegisterAdjudicatorInstruction<
  TProgram extends string = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
  TAccountGovernance extends string | AccountMeta<string> = string,
  TAccountAdjudicatorConfig extends string | AccountMeta<string> = string,
  TAccountTreasuryVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGovernance extends string
        ? WritableSignerAccount<TAccountGovernance> &
            AccountSignerMeta<TAccountGovernance>
        : TAccountGovernance,
      TAccountAdjudicatorConfig extends string
        ? WritableAccount<TAccountAdjudicatorConfig>
        : TAccountAdjudicatorConfig,
      TAccountTreasuryVault extends string
        ? WritableAccount<TAccountTreasuryVault>
        : TAccountTreasuryVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterAdjudicatorInstructionData = {
  discriminator: ReadonlyUint8Array;
  adjudicator: Address;
};

export type RegisterAdjudicatorInstructionDataArgs = { adjudicator: Address };

export function getRegisterAdjudicatorInstructionDataEncoder(): FixedSizeEncoder<RegisterAdjudicatorInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["adjudicator", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: REGISTER_ADJUDICATOR_DISCRIMINATOR,
    }),
  );
}

export function getRegisterAdjudicatorInstructionDataDecoder(): FixedSizeDecoder<RegisterAdjudicatorInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["adjudicator", getAddressDecoder()],
  ]);
}

export function getRegisterAdjudicatorInstructionDataCodec(): FixedSizeCodec<
  RegisterAdjudicatorInstructionDataArgs,
  RegisterAdjudicatorInstructionData
> {
  return combineCodec(
    getRegisterAdjudicatorInstructionDataEncoder(),
    getRegisterAdjudicatorInstructionDataDecoder(),
  );
}

export type RegisterAdjudicatorAsyncInput<
  TAccountGovernance extends string = string,
  TAccountAdjudicatorConfig extends string = string,
  TAccountTreasuryVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  governance: TransactionSigner<TAccountGovernance>;
  adjudicatorConfig?: Address<TAccountAdjudicatorConfig>;
  treasuryVault?: Address<TAccountTreasuryVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  adjudicator: RegisterAdjudicatorInstructionDataArgs["adjudicator"];
};

export async function getRegisterAdjudicatorInstructionAsync<
  TAccountGovernance extends string,
  TAccountAdjudicatorConfig extends string,
  TAccountTreasuryVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
>(
  input: RegisterAdjudicatorAsyncInput<
    TAccountGovernance,
    TAccountAdjudicatorConfig,
    TAccountTreasuryVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterAdjudicatorInstruction<
    TProgramAddress,
    TAccountGovernance,
    TAccountAdjudicatorConfig,
    TAccountTreasuryVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? DISPUTE_RESOLVER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governance: { value: input.governance ?? null, isWritable: true },
    adjudicatorConfig: {
      value: input.adjudicatorConfig ?? null,
      isWritable: true,
    },
    treasuryVault: { value: input.treasuryVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.adjudicatorConfig.value) {
    accounts.adjudicatorConfig.value = await findAdjudicatorConfigPda();
  }
  if (!accounts.treasuryVault.value) {
    accounts.treasuryVault.value = await findTreasuryVaultPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("governance", accounts.governance),
      getAccountMeta("adjudicatorConfig", accounts.adjudicatorConfig),
      getAccountMeta("treasuryVault", accounts.treasuryVault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterAdjudicatorInstructionDataEncoder().encode(
      args as RegisterAdjudicatorInstructionDataArgs,
    ),
    programAddress,
  } as RegisterAdjudicatorInstruction<
    TProgramAddress,
    TAccountGovernance,
    TAccountAdjudicatorConfig,
    TAccountTreasuryVault,
    TAccountSystemProgram
  >);
}

export type RegisterAdjudicatorInput<
  TAccountGovernance extends string = string,
  TAccountAdjudicatorConfig extends string = string,
  TAccountTreasuryVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  governance: TransactionSigner<TAccountGovernance>;
  adjudicatorConfig: Address<TAccountAdjudicatorConfig>;
  treasuryVault: Address<TAccountTreasuryVault>;
  systemProgram?: Address<TAccountSystemProgram>;
  adjudicator: RegisterAdjudicatorInstructionDataArgs["adjudicator"];
};

export function getRegisterAdjudicatorInstruction<
  TAccountGovernance extends string,
  TAccountAdjudicatorConfig extends string,
  TAccountTreasuryVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
>(
  input: RegisterAdjudicatorInput<
    TAccountGovernance,
    TAccountAdjudicatorConfig,
    TAccountTreasuryVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterAdjudicatorInstruction<
  TProgramAddress,
  TAccountGovernance,
  TAccountAdjudicatorConfig,
  TAccountTreasuryVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? DISPUTE_RESOLVER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governance: { value: input.governance ?? null, isWritable: true },
    adjudicatorConfig: {
      value: input.adjudicatorConfig ?? null,
      isWritable: true,
    },
    treasuryVault: { value: input.treasuryVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("governance", accounts.governance),
      getAccountMeta("adjudicatorConfig", accounts.adjudicatorConfig),
      getAccountMeta("treasuryVault", accounts.treasuryVault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterAdjudicatorInstructionDataEncoder().encode(
      args as RegisterAdjudicatorInstructionDataArgs,
    ),
    programAddress,
  } as RegisterAdjudicatorInstruction<
    TProgramAddress,
    TAccountGovernance,
    TAccountAdjudicatorConfig,
    TAccountTreasuryVault,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterAdjudicatorInstruction<
  TProgram extends string = typeof DISPUTE_RESOLVER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    governance: TAccountMetas[0];
    adjudicatorConfig: TAccountMetas[1];
    treasuryVault: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RegisterAdjudicatorInstructionData;
};

export function parseRegisterAdjudicatorInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterAdjudicatorInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      governance: getNextAccount(),
      adjudicatorConfig: getNextAccount(),
      treasuryVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterAdjudicatorInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
