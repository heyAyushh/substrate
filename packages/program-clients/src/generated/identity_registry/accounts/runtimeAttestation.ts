/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const RUNTIME_ATTESTATION_DISCRIMINATOR = new Uint8Array([
  1, 86, 253, 43, 237, 246, 16, 16,
]);

export function getRuntimeAttestationDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RUNTIME_ATTESTATION_DISCRIMINATOR,
  );
}

export type RuntimeAttestation = {
  discriminator: ReadonlyUint8Array;
  identity: Address;
  runtimeCommit: ReadonlyUint8Array;
  runtimeAuthority: Address;
  validFromSlot: bigint;
  bump: number;
};

export type RuntimeAttestationArgs = {
  identity: Address;
  runtimeCommit: ReadonlyUint8Array;
  runtimeAuthority: Address;
  validFromSlot: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link RuntimeAttestationArgs} account data. */
export function getRuntimeAttestationEncoder(): FixedSizeEncoder<RuntimeAttestationArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["identity", getAddressEncoder()],
      ["runtimeCommit", fixEncoderSize(getBytesEncoder(), 32)],
      ["runtimeAuthority", getAddressEncoder()],
      ["validFromSlot", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: RUNTIME_ATTESTATION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link RuntimeAttestation} account data. */
export function getRuntimeAttestationDecoder(): FixedSizeDecoder<RuntimeAttestation> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["identity", getAddressDecoder()],
    ["runtimeCommit", fixDecoderSize(getBytesDecoder(), 32)],
    ["runtimeAuthority", getAddressDecoder()],
    ["validFromSlot", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link RuntimeAttestation} account data. */
export function getRuntimeAttestationCodec(): FixedSizeCodec<
  RuntimeAttestationArgs,
  RuntimeAttestation
> {
  return combineCodec(
    getRuntimeAttestationEncoder(),
    getRuntimeAttestationDecoder(),
  );
}

export function decodeRuntimeAttestation<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<RuntimeAttestation, TAddress>;
export function decodeRuntimeAttestation<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<RuntimeAttestation, TAddress>;
export function decodeRuntimeAttestation<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<RuntimeAttestation, TAddress>
  | MaybeAccount<RuntimeAttestation, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRuntimeAttestationDecoder(),
  );
}

export async function fetchRuntimeAttestation<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<RuntimeAttestation, TAddress>> {
  const maybeAccount = await fetchMaybeRuntimeAttestation(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRuntimeAttestation<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<RuntimeAttestation, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRuntimeAttestation(maybeAccount);
}

export async function fetchAllRuntimeAttestation(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<RuntimeAttestation>[]> {
  const maybeAccounts = await fetchAllMaybeRuntimeAttestation(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRuntimeAttestation(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<RuntimeAttestation>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeRuntimeAttestation(maybeAccount),
  );
}

export function getRuntimeAttestationSize(): number {
  return 113;
}
