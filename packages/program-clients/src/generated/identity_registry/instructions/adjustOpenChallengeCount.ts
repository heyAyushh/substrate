/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI8Decoder,
  getI8Encoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { IDENTITY_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const ADJUST_OPEN_CHALLENGE_COUNT_DISCRIMINATOR = new Uint8Array([
  98, 214, 169, 234, 33, 117, 117, 170,
]);

export function getAdjustOpenChallengeCountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADJUST_OPEN_CHALLENGE_COUNT_DISCRIMINATOR,
  );
}

export type AdjustOpenChallengeCountInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountChallengeAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountChallengeAuthority extends string
        ? ReadonlySignerAccount<TAccountChallengeAuthority> &
            AccountSignerMeta<TAccountChallengeAuthority>
        : TAccountChallengeAuthority,
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      ...TRemainingAccounts,
    ]
  >;

export type AdjustOpenChallengeCountInstructionData = {
  discriminator: ReadonlyUint8Array;
  delta: number;
};

export type AdjustOpenChallengeCountInstructionDataArgs = { delta: number };

export function getAdjustOpenChallengeCountInstructionDataEncoder(): FixedSizeEncoder<AdjustOpenChallengeCountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["delta", getI8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: ADJUST_OPEN_CHALLENGE_COUNT_DISCRIMINATOR,
    }),
  );
}

export function getAdjustOpenChallengeCountInstructionDataDecoder(): FixedSizeDecoder<AdjustOpenChallengeCountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["delta", getI8Decoder()],
  ]);
}

export function getAdjustOpenChallengeCountInstructionDataCodec(): FixedSizeCodec<
  AdjustOpenChallengeCountInstructionDataArgs,
  AdjustOpenChallengeCountInstructionData
> {
  return combineCodec(
    getAdjustOpenChallengeCountInstructionDataEncoder(),
    getAdjustOpenChallengeCountInstructionDataDecoder(),
  );
}

export type AdjustOpenChallengeCountInput<
  TAccountChallengeAuthority extends string = string,
  TAccountIdentity extends string = string,
> = {
  challengeAuthority: TransactionSigner<TAccountChallengeAuthority>;
  identity: Address<TAccountIdentity>;
  delta: AdjustOpenChallengeCountInstructionDataArgs["delta"];
};

export function getAdjustOpenChallengeCountInstruction<
  TAccountChallengeAuthority extends string,
  TAccountIdentity extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AdjustOpenChallengeCountInput<
    TAccountChallengeAuthority,
    TAccountIdentity
  >,
  config?: { programAddress?: TProgramAddress },
): AdjustOpenChallengeCountInstruction<
  TProgramAddress,
  TAccountChallengeAuthority,
  TAccountIdentity
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    challengeAuthority: {
      value: input.challengeAuthority ?? null,
      isWritable: false,
    },
    identity: { value: input.identity ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("challengeAuthority", accounts.challengeAuthority),
      getAccountMeta("identity", accounts.identity),
    ],
    data: getAdjustOpenChallengeCountInstructionDataEncoder().encode(
      args as AdjustOpenChallengeCountInstructionDataArgs,
    ),
    programAddress,
  } as AdjustOpenChallengeCountInstruction<
    TProgramAddress,
    TAccountChallengeAuthority,
    TAccountIdentity
  >);
}

export type ParsedAdjustOpenChallengeCountInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    challengeAuthority: TAccountMetas[0];
    identity: TAccountMetas[1];
  };
  data: AdjustOpenChallengeCountInstructionData;
};

export function parseAdjustOpenChallengeCountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdjustOpenChallengeCountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      challengeAuthority: getNextAccount(),
      identity: getNextAccount(),
    },
    data: getAdjustOpenChallengeCountInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
