/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI8Decoder,
  getI8Encoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { IDENTITY_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const ADJUST_OPEN_TASK_COUNT_DISCRIMINATOR = new Uint8Array([
  1, 159, 151, 218, 233, 165, 158, 34,
]);

export function getAdjustOpenTaskCountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADJUST_OPEN_TASK_COUNT_DISCRIMINATOR,
  );
}

export type AdjustOpenTaskCountInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      ...TRemainingAccounts,
    ]
  >;

export type AdjustOpenTaskCountInstructionData = {
  discriminator: ReadonlyUint8Array;
  delta: number;
};

export type AdjustOpenTaskCountInstructionDataArgs = { delta: number };

export function getAdjustOpenTaskCountInstructionDataEncoder(): FixedSizeEncoder<AdjustOpenTaskCountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["delta", getI8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: ADJUST_OPEN_TASK_COUNT_DISCRIMINATOR,
    }),
  );
}

export function getAdjustOpenTaskCountInstructionDataDecoder(): FixedSizeDecoder<AdjustOpenTaskCountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["delta", getI8Decoder()],
  ]);
}

export function getAdjustOpenTaskCountInstructionDataCodec(): FixedSizeCodec<
  AdjustOpenTaskCountInstructionDataArgs,
  AdjustOpenTaskCountInstructionData
> {
  return combineCodec(
    getAdjustOpenTaskCountInstructionDataEncoder(),
    getAdjustOpenTaskCountInstructionDataDecoder(),
  );
}

export type AdjustOpenTaskCountInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  delta: AdjustOpenTaskCountInstructionDataArgs["delta"];
};

export function getAdjustOpenTaskCountInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AdjustOpenTaskCountInput<TAccountAuthority, TAccountIdentity>,
  config?: { programAddress?: TProgramAddress },
): AdjustOpenTaskCountInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    identity: { value: input.identity ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
    ],
    data: getAdjustOpenTaskCountInstructionDataEncoder().encode(
      args as AdjustOpenTaskCountInstructionDataArgs,
    ),
    programAddress,
  } as AdjustOpenTaskCountInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity
  >);
}

export type ParsedAdjustOpenTaskCountInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
  };
  data: AdjustOpenTaskCountInstructionData;
};

export function parseAdjustOpenTaskCountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdjustOpenTaskCountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), identity: getNextAccount() },
    data: getAdjustOpenTaskCountInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
