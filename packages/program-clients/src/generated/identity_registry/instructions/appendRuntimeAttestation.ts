/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findRuntimeAttestationPda } from "../pdas";
import { IDENTITY_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const APPEND_RUNTIME_ATTESTATION_DISCRIMINATOR = new Uint8Array([
  252, 109, 251, 89, 114, 84, 217, 103,
]);

export function getAppendRuntimeAttestationDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPEND_RUNTIME_ATTESTATION_DISCRIMINATOR,
  );
}

export type AppendRuntimeAttestationInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountRuntimeAttestation extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? ReadonlyAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountRuntimeAttestation extends string
        ? WritableAccount<TAccountRuntimeAttestation>
        : TAccountRuntimeAttestation,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AppendRuntimeAttestationInstructionData = {
  discriminator: ReadonlyUint8Array;
  runtimeCommit: ReadonlyUint8Array;
  runtimeAuthority: Address;
};

export type AppendRuntimeAttestationInstructionDataArgs = {
  runtimeCommit: ReadonlyUint8Array;
  runtimeAuthority: Address;
};

export function getAppendRuntimeAttestationInstructionDataEncoder(): FixedSizeEncoder<AppendRuntimeAttestationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["runtimeCommit", fixEncoderSize(getBytesEncoder(), 32)],
      ["runtimeAuthority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: APPEND_RUNTIME_ATTESTATION_DISCRIMINATOR,
    }),
  );
}

export function getAppendRuntimeAttestationInstructionDataDecoder(): FixedSizeDecoder<AppendRuntimeAttestationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["runtimeCommit", fixDecoderSize(getBytesDecoder(), 32)],
    ["runtimeAuthority", getAddressDecoder()],
  ]);
}

export function getAppendRuntimeAttestationInstructionDataCodec(): FixedSizeCodec<
  AppendRuntimeAttestationInstructionDataArgs,
  AppendRuntimeAttestationInstructionData
> {
  return combineCodec(
    getAppendRuntimeAttestationInstructionDataEncoder(),
    getAppendRuntimeAttestationInstructionDataDecoder(),
  );
}

export type AppendRuntimeAttestationAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountRuntimeAttestation extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  runtimeAttestation?: Address<TAccountRuntimeAttestation>;
  systemProgram?: Address<TAccountSystemProgram>;
  runtimeCommit: AppendRuntimeAttestationInstructionDataArgs["runtimeCommit"];
  runtimeAuthority: AppendRuntimeAttestationInstructionDataArgs["runtimeAuthority"];
};

export async function getAppendRuntimeAttestationInstructionAsync<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountRuntimeAttestation extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AppendRuntimeAttestationAsyncInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountRuntimeAttestation,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AppendRuntimeAttestationInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountRuntimeAttestation,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    runtimeAttestation: {
      value: input.runtimeAttestation ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.runtimeAttestation.value) {
    accounts.runtimeAttestation.value = await findRuntimeAttestationPda({
      identity: getAddressFromResolvedInstructionAccount(
        "identity",
        accounts.identity.value,
      ),
      runtimeCommit: getNonNullResolvedInstructionInput(
        "runtimeCommit",
        args.runtimeCommit,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("runtimeAttestation", accounts.runtimeAttestation),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAppendRuntimeAttestationInstructionDataEncoder().encode(
      args as AppendRuntimeAttestationInstructionDataArgs,
    ),
    programAddress,
  } as AppendRuntimeAttestationInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountRuntimeAttestation,
    TAccountSystemProgram
  >);
}

export type AppendRuntimeAttestationInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountRuntimeAttestation extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  runtimeAttestation: Address<TAccountRuntimeAttestation>;
  systemProgram?: Address<TAccountSystemProgram>;
  runtimeCommit: AppendRuntimeAttestationInstructionDataArgs["runtimeCommit"];
  runtimeAuthority: AppendRuntimeAttestationInstructionDataArgs["runtimeAuthority"];
};

export function getAppendRuntimeAttestationInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountRuntimeAttestation extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AppendRuntimeAttestationInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountRuntimeAttestation,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AppendRuntimeAttestationInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity,
  TAccountRuntimeAttestation,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    runtimeAttestation: {
      value: input.runtimeAttestation ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("runtimeAttestation", accounts.runtimeAttestation),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAppendRuntimeAttestationInstructionDataEncoder().encode(
      args as AppendRuntimeAttestationInstructionDataArgs,
    ),
    programAddress,
  } as AppendRuntimeAttestationInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountRuntimeAttestation,
    TAccountSystemProgram
  >);
}

export type ParsedAppendRuntimeAttestationInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
    runtimeAttestation: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AppendRuntimeAttestationInstructionData;
};

export function parseAppendRuntimeAttestationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAppendRuntimeAttestationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      identity: getNextAccount(),
      runtimeAttestation: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAppendRuntimeAttestationInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
