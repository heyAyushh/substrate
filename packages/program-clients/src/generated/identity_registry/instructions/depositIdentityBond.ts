/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findIdentityBondPda } from "../pdas";
import { IDENTITY_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const DEPOSIT_IDENTITY_BOND_DISCRIMINATOR = new Uint8Array([
  197, 18, 126, 185, 0, 101, 73, 52,
]);

export function getDepositIdentityBondDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEPOSIT_IDENTITY_BOND_DISCRIMINATOR,
  );
}

export type DepositIdentityBondInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountIdentityBond extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountIdentityBond extends string
        ? WritableAccount<TAccountIdentityBond>
        : TAccountIdentityBond,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DepositIdentityBondInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type DepositIdentityBondInstructionDataArgs = {};

export function getDepositIdentityBondInstructionDataEncoder(): FixedSizeEncoder<DepositIdentityBondInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: DEPOSIT_IDENTITY_BOND_DISCRIMINATOR,
    }),
  );
}

export function getDepositIdentityBondInstructionDataDecoder(): FixedSizeDecoder<DepositIdentityBondInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDepositIdentityBondInstructionDataCodec(): FixedSizeCodec<
  DepositIdentityBondInstructionDataArgs,
  DepositIdentityBondInstructionData
> {
  return combineCodec(
    getDepositIdentityBondInstructionDataEncoder(),
    getDepositIdentityBondInstructionDataDecoder(),
  );
}

export type DepositIdentityBondAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountIdentityBond extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  identityBond?: Address<TAccountIdentityBond>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getDepositIdentityBondInstructionAsync<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountIdentityBond extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: DepositIdentityBondAsyncInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DepositIdentityBondInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: true },
    identityBond: { value: input.identityBond ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.identityBond.value) {
    accounts.identityBond.value = await findIdentityBondPda({
      identity: getAddressFromResolvedInstructionAccount(
        "identity",
        accounts.identity.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("identityBond", accounts.identityBond),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDepositIdentityBondInstructionDataEncoder().encode({}),
    programAddress,
  } as DepositIdentityBondInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond,
    TAccountSystemProgram
  >);
}

export type DepositIdentityBondInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountIdentityBond extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  identityBond: Address<TAccountIdentityBond>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getDepositIdentityBondInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountIdentityBond extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: DepositIdentityBondInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DepositIdentityBondInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity,
  TAccountIdentityBond,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: true },
    identityBond: { value: input.identityBond ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("identityBond", accounts.identityBond),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getDepositIdentityBondInstructionDataEncoder().encode({}),
    programAddress,
  } as DepositIdentityBondInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond,
    TAccountSystemProgram
  >);
}

export type ParsedDepositIdentityBondInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
    identityBond: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: DepositIdentityBondInstructionData;
};

export function parseDepositIdentityBondInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDepositIdentityBondInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      identity: getNextAccount(),
      identityBond: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDepositIdentityBondInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
