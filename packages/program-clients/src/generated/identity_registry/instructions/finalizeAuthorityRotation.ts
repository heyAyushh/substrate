/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findPendingRotationPda } from "../pdas";
import { IDENTITY_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const FINALIZE_AUTHORITY_ROTATION_DISCRIMINATOR = new Uint8Array([
  136, 245, 112, 52, 74, 191, 74, 57,
]);

export function getFinalizeAuthorityRotationDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINALIZE_AUTHORITY_ROTATION_DISCRIMINATOR,
  );
}

export type FinalizeAuthorityRotationInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountPendingRotation extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountPendingRotation extends string
        ? WritableAccount<TAccountPendingRotation>
        : TAccountPendingRotation,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizeAuthorityRotationInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FinalizeAuthorityRotationInstructionDataArgs = {};

export function getFinalizeAuthorityRotationInstructionDataEncoder(): FixedSizeEncoder<FinalizeAuthorityRotationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: FINALIZE_AUTHORITY_ROTATION_DISCRIMINATOR,
    }),
  );
}

export function getFinalizeAuthorityRotationInstructionDataDecoder(): FixedSizeDecoder<FinalizeAuthorityRotationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinalizeAuthorityRotationInstructionDataCodec(): FixedSizeCodec<
  FinalizeAuthorityRotationInstructionDataArgs,
  FinalizeAuthorityRotationInstructionData
> {
  return combineCodec(
    getFinalizeAuthorityRotationInstructionDataEncoder(),
    getFinalizeAuthorityRotationInstructionDataDecoder(),
  );
}

export type FinalizeAuthorityRotationAsyncInput<
  TAccountCaller extends string = string,
  TAccountIdentity extends string = string,
  TAccountPendingRotation extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  identity: Address<TAccountIdentity>;
  pendingRotation?: Address<TAccountPendingRotation>;
};

export async function getFinalizeAuthorityRotationInstructionAsync<
  TAccountCaller extends string,
  TAccountIdentity extends string,
  TAccountPendingRotation extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: FinalizeAuthorityRotationAsyncInput<
    TAccountCaller,
    TAccountIdentity,
    TAccountPendingRotation
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  FinalizeAuthorityRotationInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountIdentity,
    TAccountPendingRotation
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: true },
    pendingRotation: { value: input.pendingRotation ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.pendingRotation.value) {
    accounts.pendingRotation.value = await findPendingRotationPda({
      identity: getAddressFromResolvedInstructionAccount(
        "identity",
        accounts.identity.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("pendingRotation", accounts.pendingRotation),
    ],
    data: getFinalizeAuthorityRotationInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeAuthorityRotationInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountIdentity,
    TAccountPendingRotation
  >);
}

export type FinalizeAuthorityRotationInput<
  TAccountCaller extends string = string,
  TAccountIdentity extends string = string,
  TAccountPendingRotation extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  identity: Address<TAccountIdentity>;
  pendingRotation: Address<TAccountPendingRotation>;
};

export function getFinalizeAuthorityRotationInstruction<
  TAccountCaller extends string,
  TAccountIdentity extends string,
  TAccountPendingRotation extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: FinalizeAuthorityRotationInput<
    TAccountCaller,
    TAccountIdentity,
    TAccountPendingRotation
  >,
  config?: { programAddress?: TProgramAddress },
): FinalizeAuthorityRotationInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountIdentity,
  TAccountPendingRotation
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: true },
    pendingRotation: { value: input.pendingRotation ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("pendingRotation", accounts.pendingRotation),
    ],
    data: getFinalizeAuthorityRotationInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeAuthorityRotationInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountIdentity,
    TAccountPendingRotation
  >);
}

export type ParsedFinalizeAuthorityRotationInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    identity: TAccountMetas[1];
    pendingRotation: TAccountMetas[2];
  };
  data: FinalizeAuthorityRotationInstructionData;
};

export function parseFinalizeAuthorityRotationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFinalizeAuthorityRotationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      identity: getNextAccount(),
      pendingRotation: getNextAccount(),
    },
    data: getFinalizeAuthorityRotationInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
