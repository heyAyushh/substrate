/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findGuardianSetPda } from "../pdas";
import { IDENTITY_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_GUARDIAN_SET_DISCRIMINATOR = new Uint8Array([
  54, 1, 142, 249, 150, 135, 55, 127,
]);

export function getInitializeGuardianSetDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_GUARDIAN_SET_DISCRIMINATOR,
  );
}

export type InitializeGuardianSetInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountGuardianSet extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountGuardianSet extends string
        ? WritableAccount<TAccountGuardianSet>
        : TAccountGuardianSet,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeGuardianSetInstructionData = {
  discriminator: ReadonlyUint8Array;
  guardians: Array<Address>;
  threshold: number;
};

export type InitializeGuardianSetInstructionDataArgs = {
  guardians: Array<Address>;
  threshold: number;
};

export function getInitializeGuardianSetInstructionDataEncoder(): Encoder<InitializeGuardianSetInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["guardians", getArrayEncoder(getAddressEncoder())],
      ["threshold", getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_GUARDIAN_SET_DISCRIMINATOR,
    }),
  );
}

export function getInitializeGuardianSetInstructionDataDecoder(): Decoder<InitializeGuardianSetInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["guardians", getArrayDecoder(getAddressDecoder())],
    ["threshold", getU8Decoder()],
  ]);
}

export function getInitializeGuardianSetInstructionDataCodec(): Codec<
  InitializeGuardianSetInstructionDataArgs,
  InitializeGuardianSetInstructionData
> {
  return combineCodec(
    getInitializeGuardianSetInstructionDataEncoder(),
    getInitializeGuardianSetInstructionDataDecoder(),
  );
}

export type InitializeGuardianSetAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountGuardianSet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  guardianSet?: Address<TAccountGuardianSet>;
  systemProgram?: Address<TAccountSystemProgram>;
  guardians: InitializeGuardianSetInstructionDataArgs["guardians"];
  threshold: InitializeGuardianSetInstructionDataArgs["threshold"];
};

export async function getInitializeGuardianSetInstructionAsync<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountGuardianSet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: InitializeGuardianSetAsyncInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountGuardianSet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeGuardianSetInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountGuardianSet,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: true },
    guardianSet: { value: input.guardianSet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.guardianSet.value) {
    accounts.guardianSet.value = await findGuardianSetPda({
      identity: getAddressFromResolvedInstructionAccount(
        "identity",
        accounts.identity.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("guardianSet", accounts.guardianSet),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeGuardianSetInstructionDataEncoder().encode(
      args as InitializeGuardianSetInstructionDataArgs,
    ),
    programAddress,
  } as InitializeGuardianSetInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountGuardianSet,
    TAccountSystemProgram
  >);
}

export type InitializeGuardianSetInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountGuardianSet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  guardianSet: Address<TAccountGuardianSet>;
  systemProgram?: Address<TAccountSystemProgram>;
  guardians: InitializeGuardianSetInstructionDataArgs["guardians"];
  threshold: InitializeGuardianSetInstructionDataArgs["threshold"];
};

export function getInitializeGuardianSetInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountGuardianSet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: InitializeGuardianSetInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountGuardianSet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeGuardianSetInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity,
  TAccountGuardianSet,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: true },
    guardianSet: { value: input.guardianSet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("guardianSet", accounts.guardianSet),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeGuardianSetInstructionDataEncoder().encode(
      args as InitializeGuardianSetInstructionDataArgs,
    ),
    programAddress,
  } as InitializeGuardianSetInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountGuardianSet,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeGuardianSetInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
    guardianSet: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitializeGuardianSetInstructionData;
};

export function parseInitializeGuardianSetInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeGuardianSetInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      identity: getNextAccount(),
      guardianSet: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeGuardianSetInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
