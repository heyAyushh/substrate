/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { IDENTITY_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_POLICY_ROOT_DISCRIMINATOR = new Uint8Array([
  204, 72, 225, 189, 180, 164, 143, 74,
]);

export function getUpdatePolicyRootDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_POLICY_ROOT_DISCRIMINATOR,
  );
}

export type UpdatePolicyRootInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdatePolicyRootInstructionData = {
  discriminator: ReadonlyUint8Array;
  newRoot: ReadonlyUint8Array;
};

export type UpdatePolicyRootInstructionDataArgs = {
  newRoot: ReadonlyUint8Array;
};

export function getUpdatePolicyRootInstructionDataEncoder(): FixedSizeEncoder<UpdatePolicyRootInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newRoot", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_POLICY_ROOT_DISCRIMINATOR }),
  );
}

export function getUpdatePolicyRootInstructionDataDecoder(): FixedSizeDecoder<UpdatePolicyRootInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newRoot", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getUpdatePolicyRootInstructionDataCodec(): FixedSizeCodec<
  UpdatePolicyRootInstructionDataArgs,
  UpdatePolicyRootInstructionData
> {
  return combineCodec(
    getUpdatePolicyRootInstructionDataEncoder(),
    getUpdatePolicyRootInstructionDataDecoder(),
  );
}

export type UpdatePolicyRootInput<
  TAccountIdentity extends string = string,
  TAccountAuthority extends string = string,
> = {
  identity: Address<TAccountIdentity>;
  authority: TransactionSigner<TAccountAuthority>;
  newRoot: UpdatePolicyRootInstructionDataArgs["newRoot"];
};

export function getUpdatePolicyRootInstruction<
  TAccountIdentity extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: UpdatePolicyRootInput<TAccountIdentity, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): UpdatePolicyRootInstruction<
  TProgramAddress,
  TAccountIdentity,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    identity: { value: input.identity ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdatePolicyRootInstructionDataEncoder().encode(
      args as UpdatePolicyRootInstructionDataArgs,
    ),
    programAddress,
  } as UpdatePolicyRootInstruction<
    TProgramAddress,
    TAccountIdentity,
    TAccountAuthority
  >);
}

export type ParsedUpdatePolicyRootInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    identity: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdatePolicyRootInstructionData;
};

export function parseUpdatePolicyRootInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdatePolicyRootInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { identity: getNextAccount(), authority: getNextAccount() },
    data: getUpdatePolicyRootInstructionDataDecoder().decode(instruction.data),
  };
}
