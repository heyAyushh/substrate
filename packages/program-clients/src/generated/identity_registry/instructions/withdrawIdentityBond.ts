/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findIdentityBondPda } from "../pdas";
import { IDENTITY_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const WITHDRAW_IDENTITY_BOND_DISCRIMINATOR = new Uint8Array([
  132, 221, 91, 95, 189, 217, 237, 192,
]);

export function getWithdrawIdentityBondDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WITHDRAW_IDENTITY_BOND_DISCRIMINATOR,
  );
}

export type WithdrawIdentityBondInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountIdentityBond extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountIdentityBond extends string
        ? WritableAccount<TAccountIdentityBond>
        : TAccountIdentityBond,
      ...TRemainingAccounts,
    ]
  >;

export type WithdrawIdentityBondInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type WithdrawIdentityBondInstructionDataArgs = {};

export function getWithdrawIdentityBondInstructionDataEncoder(): FixedSizeEncoder<WithdrawIdentityBondInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: WITHDRAW_IDENTITY_BOND_DISCRIMINATOR,
    }),
  );
}

export function getWithdrawIdentityBondInstructionDataDecoder(): FixedSizeDecoder<WithdrawIdentityBondInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getWithdrawIdentityBondInstructionDataCodec(): FixedSizeCodec<
  WithdrawIdentityBondInstructionDataArgs,
  WithdrawIdentityBondInstructionData
> {
  return combineCodec(
    getWithdrawIdentityBondInstructionDataEncoder(),
    getWithdrawIdentityBondInstructionDataDecoder(),
  );
}

export type WithdrawIdentityBondAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountIdentityBond extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  identityBond?: Address<TAccountIdentityBond>;
};

export async function getWithdrawIdentityBondInstructionAsync<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountIdentityBond extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: WithdrawIdentityBondAsyncInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  WithdrawIdentityBondInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: true },
    identityBond: { value: input.identityBond ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.identityBond.value) {
    accounts.identityBond.value = await findIdentityBondPda({
      identity: getAddressFromResolvedInstructionAccount(
        "identity",
        accounts.identity.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("identityBond", accounts.identityBond),
    ],
    data: getWithdrawIdentityBondInstructionDataEncoder().encode({}),
    programAddress,
  } as WithdrawIdentityBondInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond
  >);
}

export type WithdrawIdentityBondInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountIdentityBond extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  identityBond: Address<TAccountIdentityBond>;
};

export function getWithdrawIdentityBondInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountIdentityBond extends string,
  TProgramAddress extends Address = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
>(
  input: WithdrawIdentityBondInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond
  >,
  config?: { programAddress?: TProgramAddress },
): WithdrawIdentityBondInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity,
  TAccountIdentityBond
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? IDENTITY_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: true },
    identityBond: { value: input.identityBond ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("identityBond", accounts.identityBond),
    ],
    data: getWithdrawIdentityBondInstructionDataEncoder().encode({}),
    programAddress,
  } as WithdrawIdentityBondInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountIdentityBond
  >);
}

export type ParsedWithdrawIdentityBondInstruction<
  TProgram extends string = typeof IDENTITY_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
    identityBond: TAccountMetas[2];
  };
  data: WithdrawIdentityBondInstructionData;
};

export function parseWithdrawIdentityBondInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedWithdrawIdentityBondInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      identity: getNextAccount(),
      identityBond: getNextAccount(),
    },
    data: getWithdrawIdentityBondInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
