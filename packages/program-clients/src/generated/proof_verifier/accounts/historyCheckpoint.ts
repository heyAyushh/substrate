/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const HISTORY_CHECKPOINT_DISCRIMINATOR = new Uint8Array([
  199, 167, 157, 116, 216, 134, 177, 9,
]);

export function getHistoryCheckpointDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    HISTORY_CHECKPOINT_DISCRIMINATOR,
  );
}

export type HistoryCheckpoint = {
  discriminator: ReadonlyUint8Array;
  identity: Address;
  epoch: bigint;
  imported: boolean;
  root: ReadonlyUint8Array;
  previousRoot: ReadonlyUint8Array;
  leafCount: bigint;
  latestCommittedReceipt: Address;
  latestTask: Address;
  latestSequence: bigint;
  frontier: Array<ReadonlyUint8Array>;
  bump: number;
};

export type HistoryCheckpointArgs = {
  identity: Address;
  epoch: number | bigint;
  imported: boolean;
  root: ReadonlyUint8Array;
  previousRoot: ReadonlyUint8Array;
  leafCount: number | bigint;
  latestCommittedReceipt: Address;
  latestTask: Address;
  latestSequence: number | bigint;
  frontier: Array<ReadonlyUint8Array>;
  bump: number;
};

/** Gets the encoder for {@link HistoryCheckpointArgs} account data. */
export function getHistoryCheckpointEncoder(): FixedSizeEncoder<HistoryCheckpointArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["identity", getAddressEncoder()],
      ["epoch", getU64Encoder()],
      ["imported", getBooleanEncoder()],
      ["root", fixEncoderSize(getBytesEncoder(), 32)],
      ["previousRoot", fixEncoderSize(getBytesEncoder(), 32)],
      ["leafCount", getU64Encoder()],
      ["latestCommittedReceipt", getAddressEncoder()],
      ["latestTask", getAddressEncoder()],
      ["latestSequence", getU64Encoder()],
      [
        "frontier",
        getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32), { size: 32 }),
      ],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: HISTORY_CHECKPOINT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link HistoryCheckpoint} account data. */
export function getHistoryCheckpointDecoder(): FixedSizeDecoder<HistoryCheckpoint> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["identity", getAddressDecoder()],
    ["epoch", getU64Decoder()],
    ["imported", getBooleanDecoder()],
    ["root", fixDecoderSize(getBytesDecoder(), 32)],
    ["previousRoot", fixDecoderSize(getBytesDecoder(), 32)],
    ["leafCount", getU64Decoder()],
    ["latestCommittedReceipt", getAddressDecoder()],
    ["latestTask", getAddressDecoder()],
    ["latestSequence", getU64Decoder()],
    [
      "frontier",
      getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32), { size: 32 }),
    ],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link HistoryCheckpoint} account data. */
export function getHistoryCheckpointCodec(): FixedSizeCodec<
  HistoryCheckpointArgs,
  HistoryCheckpoint
> {
  return combineCodec(
    getHistoryCheckpointEncoder(),
    getHistoryCheckpointDecoder(),
  );
}

export function decodeHistoryCheckpoint<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<HistoryCheckpoint, TAddress>;
export function decodeHistoryCheckpoint<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<HistoryCheckpoint, TAddress>;
export function decodeHistoryCheckpoint<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<HistoryCheckpoint, TAddress>
  | MaybeAccount<HistoryCheckpoint, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getHistoryCheckpointDecoder(),
  );
}

export async function fetchHistoryCheckpoint<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<HistoryCheckpoint, TAddress>> {
  const maybeAccount = await fetchMaybeHistoryCheckpoint(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeHistoryCheckpoint<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<HistoryCheckpoint, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeHistoryCheckpoint(maybeAccount);
}

export async function fetchAllHistoryCheckpoint(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<HistoryCheckpoint>[]> {
  const maybeAccounts = await fetchAllMaybeHistoryCheckpoint(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeHistoryCheckpoint(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<HistoryCheckpoint>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeHistoryCheckpoint(maybeAccount),
  );
}

export function getHistoryCheckpointSize(): number {
  return 1218;
}
