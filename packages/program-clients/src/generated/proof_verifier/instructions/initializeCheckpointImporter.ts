/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findCheckpointImporterPda } from "../pdas";
import { PROOF_VERIFIER_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_CHECKPOINT_IMPORTER_DISCRIMINATOR = new Uint8Array([
  74, 232, 198, 100, 88, 99, 165, 146,
]);

export function getInitializeCheckpointImporterDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_CHECKPOINT_IMPORTER_DISCRIMINATOR,
  );
}

export type InitializeCheckpointImporterInstruction<
  TProgram extends string = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountCheckpointImporter extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountCheckpointImporter extends string
        ? WritableAccount<TAccountCheckpointImporter>
        : TAccountCheckpointImporter,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeCheckpointImporterInstructionData = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
};

export type InitializeCheckpointImporterInstructionDataArgs = {
  authority: Address;
};

export function getInitializeCheckpointImporterInstructionDataEncoder(): FixedSizeEncoder<InitializeCheckpointImporterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_CHECKPOINT_IMPORTER_DISCRIMINATOR,
    }),
  );
}

export function getInitializeCheckpointImporterInstructionDataDecoder(): FixedSizeDecoder<InitializeCheckpointImporterInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
  ]);
}

export function getInitializeCheckpointImporterInstructionDataCodec(): FixedSizeCodec<
  InitializeCheckpointImporterInstructionDataArgs,
  InitializeCheckpointImporterInstructionData
> {
  return combineCodec(
    getInitializeCheckpointImporterInstructionDataEncoder(),
    getInitializeCheckpointImporterInstructionDataDecoder(),
  );
}

export type InitializeCheckpointImporterAsyncInput<
  TAccountPayer extends string = string,
  TAccountCheckpointImporter extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  checkpointImporter?: Address<TAccountCheckpointImporter>;
  systemProgram?: Address<TAccountSystemProgram>;
  authority: InitializeCheckpointImporterInstructionDataArgs["authority"];
};

export async function getInitializeCheckpointImporterInstructionAsync<
  TAccountPayer extends string,
  TAccountCheckpointImporter extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
>(
  input: InitializeCheckpointImporterAsyncInput<
    TAccountPayer,
    TAccountCheckpointImporter,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeCheckpointImporterInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountCheckpointImporter,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROOF_VERIFIER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    checkpointImporter: {
      value: input.checkpointImporter ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.checkpointImporter.value) {
    accounts.checkpointImporter.value = await findCheckpointImporterPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("checkpointImporter", accounts.checkpointImporter),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeCheckpointImporterInstructionDataEncoder().encode(
      args as InitializeCheckpointImporterInstructionDataArgs,
    ),
    programAddress,
  } as InitializeCheckpointImporterInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountCheckpointImporter,
    TAccountSystemProgram
  >);
}

export type InitializeCheckpointImporterInput<
  TAccountPayer extends string = string,
  TAccountCheckpointImporter extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  checkpointImporter: Address<TAccountCheckpointImporter>;
  systemProgram?: Address<TAccountSystemProgram>;
  authority: InitializeCheckpointImporterInstructionDataArgs["authority"];
};

export function getInitializeCheckpointImporterInstruction<
  TAccountPayer extends string,
  TAccountCheckpointImporter extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
>(
  input: InitializeCheckpointImporterInput<
    TAccountPayer,
    TAccountCheckpointImporter,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeCheckpointImporterInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountCheckpointImporter,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROOF_VERIFIER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    checkpointImporter: {
      value: input.checkpointImporter ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("checkpointImporter", accounts.checkpointImporter),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeCheckpointImporterInstructionDataEncoder().encode(
      args as InitializeCheckpointImporterInstructionDataArgs,
    ),
    programAddress,
  } as InitializeCheckpointImporterInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountCheckpointImporter,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeCheckpointImporterInstruction<
  TProgram extends string = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    checkpointImporter: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeCheckpointImporterInstructionData;
};

export function parseInitializeCheckpointImporterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeCheckpointImporterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      checkpointImporter: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeCheckpointImporterInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
