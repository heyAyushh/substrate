/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findHistoryUpdaterPda } from "../pdas";
import { PROOF_VERIFIER_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_HISTORY_UPDATER_DISCRIMINATOR = new Uint8Array([
  51, 186, 92, 155, 29, 217, 177, 178,
]);

export function getInitializeHistoryUpdaterDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_HISTORY_UPDATER_DISCRIMINATOR,
  );
}

export type InitializeHistoryUpdaterInstruction<
  TProgram extends string = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountHistoryUpdater extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountHistoryUpdater extends string
        ? WritableAccount<TAccountHistoryUpdater>
        : TAccountHistoryUpdater,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeHistoryUpdaterInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeHistoryUpdaterInstructionDataArgs = {};

export function getInitializeHistoryUpdaterInstructionDataEncoder(): FixedSizeEncoder<InitializeHistoryUpdaterInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_HISTORY_UPDATER_DISCRIMINATOR,
    }),
  );
}

export function getInitializeHistoryUpdaterInstructionDataDecoder(): FixedSizeDecoder<InitializeHistoryUpdaterInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeHistoryUpdaterInstructionDataCodec(): FixedSizeCodec<
  InitializeHistoryUpdaterInstructionDataArgs,
  InitializeHistoryUpdaterInstructionData
> {
  return combineCodec(
    getInitializeHistoryUpdaterInstructionDataEncoder(),
    getInitializeHistoryUpdaterInstructionDataDecoder(),
  );
}

export type InitializeHistoryUpdaterAsyncInput<
  TAccountPayer extends string = string,
  TAccountHistoryUpdater extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  historyUpdater?: Address<TAccountHistoryUpdater>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeHistoryUpdaterInstructionAsync<
  TAccountPayer extends string,
  TAccountHistoryUpdater extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
>(
  input: InitializeHistoryUpdaterAsyncInput<
    TAccountPayer,
    TAccountHistoryUpdater,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeHistoryUpdaterInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountHistoryUpdater,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROOF_VERIFIER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    historyUpdater: { value: input.historyUpdater ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.historyUpdater.value) {
    accounts.historyUpdater.value = await findHistoryUpdaterPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("historyUpdater", accounts.historyUpdater),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeHistoryUpdaterInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeHistoryUpdaterInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountHistoryUpdater,
    TAccountSystemProgram
  >);
}

export type InitializeHistoryUpdaterInput<
  TAccountPayer extends string = string,
  TAccountHistoryUpdater extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  historyUpdater: Address<TAccountHistoryUpdater>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeHistoryUpdaterInstruction<
  TAccountPayer extends string,
  TAccountHistoryUpdater extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
>(
  input: InitializeHistoryUpdaterInput<
    TAccountPayer,
    TAccountHistoryUpdater,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeHistoryUpdaterInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountHistoryUpdater,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROOF_VERIFIER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    historyUpdater: { value: input.historyUpdater ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("historyUpdater", accounts.historyUpdater),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeHistoryUpdaterInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeHistoryUpdaterInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountHistoryUpdater,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeHistoryUpdaterInstruction<
  TProgram extends string = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    historyUpdater: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeHistoryUpdaterInstructionData;
};

export function parseInitializeHistoryUpdaterInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeHistoryUpdaterInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      historyUpdater: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeHistoryUpdaterInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
