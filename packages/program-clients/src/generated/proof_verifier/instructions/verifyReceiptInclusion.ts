/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { PROOF_VERIFIER_PROGRAM_ADDRESS } from "../programs";

export const VERIFY_RECEIPT_INCLUSION_DISCRIMINATOR = new Uint8Array([
  13, 137, 24, 74, 36, 232, 151, 103,
]);

export function getVerifyReceiptInclusionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VERIFY_RECEIPT_INCLUSION_DISCRIMINATOR,
  );
}

export type VerifyReceiptInclusionInstruction<
  TProgram extends string = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
  TAccountCheckpoint extends string | AccountMeta<string> = string,
  TAccountLatestCheckpoint extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCheckpoint extends string
        ? ReadonlyAccount<TAccountCheckpoint>
        : TAccountCheckpoint,
      TAccountLatestCheckpoint extends string
        ? ReadonlyAccount<TAccountLatestCheckpoint>
        : TAccountLatestCheckpoint,
      ...TRemainingAccounts,
    ]
  >;

export type VerifyReceiptInclusionInstructionData = {
  discriminator: ReadonlyUint8Array;
  leaf: ReadonlyUint8Array;
  leafIndex: bigint;
  siblings: Array<ReadonlyUint8Array>;
};

export type VerifyReceiptInclusionInstructionDataArgs = {
  leaf: ReadonlyUint8Array;
  leafIndex: number | bigint;
  siblings: Array<ReadonlyUint8Array>;
};

export function getVerifyReceiptInclusionInstructionDataEncoder(): Encoder<VerifyReceiptInclusionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["leaf", fixEncoderSize(getBytesEncoder(), 32)],
      ["leafIndex", getU64Encoder()],
      ["siblings", getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32))],
    ]),
    (value) => ({
      ...value,
      discriminator: VERIFY_RECEIPT_INCLUSION_DISCRIMINATOR,
    }),
  );
}

export function getVerifyReceiptInclusionInstructionDataDecoder(): Decoder<VerifyReceiptInclusionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["leaf", fixDecoderSize(getBytesDecoder(), 32)],
    ["leafIndex", getU64Decoder()],
    ["siblings", getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32))],
  ]);
}

export function getVerifyReceiptInclusionInstructionDataCodec(): Codec<
  VerifyReceiptInclusionInstructionDataArgs,
  VerifyReceiptInclusionInstructionData
> {
  return combineCodec(
    getVerifyReceiptInclusionInstructionDataEncoder(),
    getVerifyReceiptInclusionInstructionDataDecoder(),
  );
}

export type VerifyReceiptInclusionInput<
  TAccountCheckpoint extends string = string,
  TAccountLatestCheckpoint extends string = string,
> = {
  checkpoint: Address<TAccountCheckpoint>;
  latestCheckpoint: Address<TAccountLatestCheckpoint>;
  leaf: VerifyReceiptInclusionInstructionDataArgs["leaf"];
  leafIndex: VerifyReceiptInclusionInstructionDataArgs["leafIndex"];
  siblings: VerifyReceiptInclusionInstructionDataArgs["siblings"];
};

export function getVerifyReceiptInclusionInstruction<
  TAccountCheckpoint extends string,
  TAccountLatestCheckpoint extends string,
  TProgramAddress extends Address = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
>(
  input: VerifyReceiptInclusionInput<
    TAccountCheckpoint,
    TAccountLatestCheckpoint
  >,
  config?: { programAddress?: TProgramAddress },
): VerifyReceiptInclusionInstruction<
  TProgramAddress,
  TAccountCheckpoint,
  TAccountLatestCheckpoint
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PROOF_VERIFIER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    checkpoint: { value: input.checkpoint ?? null, isWritable: false },
    latestCheckpoint: {
      value: input.latestCheckpoint ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("checkpoint", accounts.checkpoint),
      getAccountMeta("latestCheckpoint", accounts.latestCheckpoint),
    ],
    data: getVerifyReceiptInclusionInstructionDataEncoder().encode(
      args as VerifyReceiptInclusionInstructionDataArgs,
    ),
    programAddress,
  } as VerifyReceiptInclusionInstruction<
    TProgramAddress,
    TAccountCheckpoint,
    TAccountLatestCheckpoint
  >);
}

export type ParsedVerifyReceiptInclusionInstruction<
  TProgram extends string = typeof PROOF_VERIFIER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    checkpoint: TAccountMetas[0];
    latestCheckpoint: TAccountMetas[1];
  };
  data: VerifyReceiptInclusionInstructionData;
};

export function parseVerifyReceiptInclusionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVerifyReceiptInclusionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      checkpoint: getNextAccount(),
      latestCheckpoint: getNextAccount(),
    },
    data: getVerifyReceiptInclusionInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
