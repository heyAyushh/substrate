/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const RECEIPT_RECORD_DISCRIMINATOR = new Uint8Array([
  51, 97, 207, 106, 28, 85, 70, 40,
]);

export function getReceiptRecordDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECEIPT_RECORD_DISCRIMINATOR,
  );
}

export type ReceiptRecord = {
  discriminator: ReadonlyUint8Array;
  identity: Address;
  task: Address;
  receiptId: ReadonlyUint8Array;
  actor: Address;
  kind: number;
  sequence: bigint;
  domain: ReadonlyUint8Array;
  previousReceipt: ReadonlyUint8Array;
  payloadHash: ReadonlyUint8Array;
  viaDelegation: Address;
  auditorIdentity: Address;
  targetReceipt: Address;
  challengeReceipt: Address;
  deadlineSlot: bigint;
  round: number;
  bump: number;
};

export type ReceiptRecordArgs = {
  identity: Address;
  task: Address;
  receiptId: ReadonlyUint8Array;
  actor: Address;
  kind: number;
  sequence: number | bigint;
  domain: ReadonlyUint8Array;
  previousReceipt: ReadonlyUint8Array;
  payloadHash: ReadonlyUint8Array;
  viaDelegation: Address;
  auditorIdentity: Address;
  targetReceipt: Address;
  challengeReceipt: Address;
  deadlineSlot: number | bigint;
  round: number;
  bump: number;
};

/** Gets the encoder for {@link ReceiptRecordArgs} account data. */
export function getReceiptRecordEncoder(): FixedSizeEncoder<ReceiptRecordArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["identity", getAddressEncoder()],
      ["task", getAddressEncoder()],
      ["receiptId", fixEncoderSize(getBytesEncoder(), 32)],
      ["actor", getAddressEncoder()],
      ["kind", getU8Encoder()],
      ["sequence", getU64Encoder()],
      ["domain", fixEncoderSize(getBytesEncoder(), 32)],
      ["previousReceipt", fixEncoderSize(getBytesEncoder(), 32)],
      ["payloadHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["viaDelegation", getAddressEncoder()],
      ["auditorIdentity", getAddressEncoder()],
      ["targetReceipt", getAddressEncoder()],
      ["challengeReceipt", getAddressEncoder()],
      ["deadlineSlot", getU64Encoder()],
      ["round", getU16Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: RECEIPT_RECORD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ReceiptRecord} account data. */
export function getReceiptRecordDecoder(): FixedSizeDecoder<ReceiptRecord> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["identity", getAddressDecoder()],
    ["task", getAddressDecoder()],
    ["receiptId", fixDecoderSize(getBytesDecoder(), 32)],
    ["actor", getAddressDecoder()],
    ["kind", getU8Decoder()],
    ["sequence", getU64Decoder()],
    ["domain", fixDecoderSize(getBytesDecoder(), 32)],
    ["previousReceipt", fixDecoderSize(getBytesDecoder(), 32)],
    ["payloadHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["viaDelegation", getAddressDecoder()],
    ["auditorIdentity", getAddressDecoder()],
    ["targetReceipt", getAddressDecoder()],
    ["challengeReceipt", getAddressDecoder()],
    ["deadlineSlot", getU64Decoder()],
    ["round", getU16Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link ReceiptRecord} account data. */
export function getReceiptRecordCodec(): FixedSizeCodec<
  ReceiptRecordArgs,
  ReceiptRecord
> {
  return combineCodec(getReceiptRecordEncoder(), getReceiptRecordDecoder());
}

export function decodeReceiptRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ReceiptRecord, TAddress>;
export function decodeReceiptRecord<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ReceiptRecord, TAddress>;
export function decodeReceiptRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<ReceiptRecord, TAddress> | MaybeAccount<ReceiptRecord, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getReceiptRecordDecoder(),
  );
}

export async function fetchReceiptRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ReceiptRecord, TAddress>> {
  const maybeAccount = await fetchMaybeReceiptRecord(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeReceiptRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ReceiptRecord, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeReceiptRecord(maybeAccount);
}

export async function fetchAllReceiptRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ReceiptRecord>[]> {
  const maybeAccounts = await fetchAllMaybeReceiptRecord(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeReceiptRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ReceiptRecord>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeReceiptRecord(maybeAccount));
}

export function getReceiptRecordSize(): number {
  return 380;
}
