/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findCpiAuthorityPda, findReceiptPda } from "../pdas";
import { RECEIPT_EMITTER_PROGRAM_ADDRESS } from "../programs";

export const EMIT_DELEGATED_RECEIPT_DISCRIMINATOR = new Uint8Array([
  219, 75, 119, 117, 214, 32, 112, 4,
]);

export function getEmitDelegatedReceiptDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EMIT_DELEGATED_RECEIPT_DISCRIMINATOR,
  );
}

export type EmitDelegatedReceiptInstruction<
  TProgram extends string = typeof RECEIPT_EMITTER_PROGRAM_ADDRESS,
  TAccountDelegate extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountDelegation extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountReceipt extends string | AccountMeta<string> = string,
  TAccountDomainCatalog extends string | AccountMeta<string> = string,
  TAccountCpiAuthority extends string | AccountMeta<string> = string,
  TAccountTaskRegistryProgram extends string | AccountMeta<string> =
    "5CjbVQQgjKeCqCsyxcb4HqPpAVgB8eNXZiZovaChQ7R4",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDelegate extends string
        ? WritableSignerAccount<TAccountDelegate> &
            AccountSignerMeta<TAccountDelegate>
        : TAccountDelegate,
      TAccountIdentity extends string
        ? ReadonlyAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountDelegation extends string
        ? ReadonlyAccount<TAccountDelegation>
        : TAccountDelegation,
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountReceipt extends string
        ? WritableAccount<TAccountReceipt>
        : TAccountReceipt,
      TAccountDomainCatalog extends string
        ? ReadonlyAccount<TAccountDomainCatalog>
        : TAccountDomainCatalog,
      TAccountCpiAuthority extends string
        ? ReadonlyAccount<TAccountCpiAuthority>
        : TAccountCpiAuthority,
      TAccountTaskRegistryProgram extends string
        ? ReadonlyAccount<TAccountTaskRegistryProgram>
        : TAccountTaskRegistryProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type EmitDelegatedReceiptInstructionData = {
  discriminator: ReadonlyUint8Array;
  receiptId: ReadonlyUint8Array;
  kind: number;
  sequence: bigint;
  domain: ReadonlyUint8Array;
  previousReceipt: ReadonlyUint8Array;
  payloadHash: ReadonlyUint8Array;
};

export type EmitDelegatedReceiptInstructionDataArgs = {
  receiptId: ReadonlyUint8Array;
  kind: number;
  sequence: number | bigint;
  domain: ReadonlyUint8Array;
  previousReceipt: ReadonlyUint8Array;
  payloadHash: ReadonlyUint8Array;
};

export function getEmitDelegatedReceiptInstructionDataEncoder(): FixedSizeEncoder<EmitDelegatedReceiptInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["receiptId", fixEncoderSize(getBytesEncoder(), 32)],
      ["kind", getU8Encoder()],
      ["sequence", getU64Encoder()],
      ["domain", fixEncoderSize(getBytesEncoder(), 32)],
      ["previousReceipt", fixEncoderSize(getBytesEncoder(), 32)],
      ["payloadHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({
      ...value,
      discriminator: EMIT_DELEGATED_RECEIPT_DISCRIMINATOR,
    }),
  );
}

export function getEmitDelegatedReceiptInstructionDataDecoder(): FixedSizeDecoder<EmitDelegatedReceiptInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["receiptId", fixDecoderSize(getBytesDecoder(), 32)],
    ["kind", getU8Decoder()],
    ["sequence", getU64Decoder()],
    ["domain", fixDecoderSize(getBytesDecoder(), 32)],
    ["previousReceipt", fixDecoderSize(getBytesDecoder(), 32)],
    ["payloadHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getEmitDelegatedReceiptInstructionDataCodec(): FixedSizeCodec<
  EmitDelegatedReceiptInstructionDataArgs,
  EmitDelegatedReceiptInstructionData
> {
  return combineCodec(
    getEmitDelegatedReceiptInstructionDataEncoder(),
    getEmitDelegatedReceiptInstructionDataDecoder(),
  );
}

export type EmitDelegatedReceiptAsyncInput<
  TAccountDelegate extends string = string,
  TAccountIdentity extends string = string,
  TAccountDelegation extends string = string,
  TAccountTask extends string = string,
  TAccountReceipt extends string = string,
  TAccountDomainCatalog extends string = string,
  TAccountCpiAuthority extends string = string,
  TAccountTaskRegistryProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  delegate: TransactionSigner<TAccountDelegate>;
  identity: Address<TAccountIdentity>;
  delegation?: Address<TAccountDelegation>;
  task: Address<TAccountTask>;
  receipt?: Address<TAccountReceipt>;
  domainCatalog: Address<TAccountDomainCatalog>;
  cpiAuthority?: Address<TAccountCpiAuthority>;
  taskRegistryProgram?: Address<TAccountTaskRegistryProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  receiptId: EmitDelegatedReceiptInstructionDataArgs["receiptId"];
  kind: EmitDelegatedReceiptInstructionDataArgs["kind"];
  sequence: EmitDelegatedReceiptInstructionDataArgs["sequence"];
  domain: EmitDelegatedReceiptInstructionDataArgs["domain"];
  previousReceipt: EmitDelegatedReceiptInstructionDataArgs["previousReceipt"];
  payloadHash: EmitDelegatedReceiptInstructionDataArgs["payloadHash"];
};

export async function getEmitDelegatedReceiptInstructionAsync<
  TAccountDelegate extends string,
  TAccountIdentity extends string,
  TAccountDelegation extends string,
  TAccountTask extends string,
  TAccountReceipt extends string,
  TAccountDomainCatalog extends string,
  TAccountCpiAuthority extends string,
  TAccountTaskRegistryProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RECEIPT_EMITTER_PROGRAM_ADDRESS,
>(
  input: EmitDelegatedReceiptAsyncInput<
    TAccountDelegate,
    TAccountIdentity,
    TAccountDelegation,
    TAccountTask,
    TAccountReceipt,
    TAccountDomainCatalog,
    TAccountCpiAuthority,
    TAccountTaskRegistryProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  EmitDelegatedReceiptInstruction<
    TProgramAddress,
    TAccountDelegate,
    TAccountIdentity,
    TAccountDelegation,
    TAccountTask,
    TAccountReceipt,
    TAccountDomainCatalog,
    TAccountCpiAuthority,
    TAccountTaskRegistryProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RECEIPT_EMITTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    delegate: { value: input.delegate ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    delegation: { value: input.delegation ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: true },
    receipt: { value: input.receipt ?? null, isWritable: true },
    domainCatalog: { value: input.domainCatalog ?? null, isWritable: false },
    cpiAuthority: { value: input.cpiAuthority ?? null, isWritable: false },
    taskRegistryProgram: {
      value: input.taskRegistryProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.delegation.value) {
    accounts.delegation.value = await getProgramDerivedAddress({
      programAddress:
        "HoRjTc9J44oSqBC4DeHfDTavkR15Le8FY3qyPFy4pg49" as Address<"HoRjTc9J44oSqBC4DeHfDTavkR15Le8FY3qyPFy4pg49">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([100, 101, 108, 101, 103, 97, 116, 105, 111, 110]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "identity",
            accounts.identity.value,
          ),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "delegate",
            accounts.delegate.value,
          ),
        ),
      ],
    });
  }
  if (!accounts.receipt.value) {
    accounts.receipt.value = await findReceiptPda({
      identity: getAddressFromResolvedInstructionAccount(
        "identity",
        accounts.identity.value,
      ),
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      receiptId: getNonNullResolvedInstructionInput(
        "receiptId",
        args.receiptId,
      ),
    });
  }
  if (!accounts.cpiAuthority.value) {
    accounts.cpiAuthority.value = await findCpiAuthorityPda();
  }
  if (!accounts.taskRegistryProgram.value) {
    accounts.taskRegistryProgram.value =
      "5CjbVQQgjKeCqCsyxcb4HqPpAVgB8eNXZiZovaChQ7R4" as Address<"5CjbVQQgjKeCqCsyxcb4HqPpAVgB8eNXZiZovaChQ7R4">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("delegate", accounts.delegate),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("delegation", accounts.delegation),
      getAccountMeta("task", accounts.task),
      getAccountMeta("receipt", accounts.receipt),
      getAccountMeta("domainCatalog", accounts.domainCatalog),
      getAccountMeta("cpiAuthority", accounts.cpiAuthority),
      getAccountMeta("taskRegistryProgram", accounts.taskRegistryProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getEmitDelegatedReceiptInstructionDataEncoder().encode(
      args as EmitDelegatedReceiptInstructionDataArgs,
    ),
    programAddress,
  } as EmitDelegatedReceiptInstruction<
    TProgramAddress,
    TAccountDelegate,
    TAccountIdentity,
    TAccountDelegation,
    TAccountTask,
    TAccountReceipt,
    TAccountDomainCatalog,
    TAccountCpiAuthority,
    TAccountTaskRegistryProgram,
    TAccountSystemProgram
  >);
}

export type EmitDelegatedReceiptInput<
  TAccountDelegate extends string = string,
  TAccountIdentity extends string = string,
  TAccountDelegation extends string = string,
  TAccountTask extends string = string,
  TAccountReceipt extends string = string,
  TAccountDomainCatalog extends string = string,
  TAccountCpiAuthority extends string = string,
  TAccountTaskRegistryProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  delegate: TransactionSigner<TAccountDelegate>;
  identity: Address<TAccountIdentity>;
  delegation: Address<TAccountDelegation>;
  task: Address<TAccountTask>;
  receipt: Address<TAccountReceipt>;
  domainCatalog: Address<TAccountDomainCatalog>;
  cpiAuthority: Address<TAccountCpiAuthority>;
  taskRegistryProgram?: Address<TAccountTaskRegistryProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  receiptId: EmitDelegatedReceiptInstructionDataArgs["receiptId"];
  kind: EmitDelegatedReceiptInstructionDataArgs["kind"];
  sequence: EmitDelegatedReceiptInstructionDataArgs["sequence"];
  domain: EmitDelegatedReceiptInstructionDataArgs["domain"];
  previousReceipt: EmitDelegatedReceiptInstructionDataArgs["previousReceipt"];
  payloadHash: EmitDelegatedReceiptInstructionDataArgs["payloadHash"];
};

export function getEmitDelegatedReceiptInstruction<
  TAccountDelegate extends string,
  TAccountIdentity extends string,
  TAccountDelegation extends string,
  TAccountTask extends string,
  TAccountReceipt extends string,
  TAccountDomainCatalog extends string,
  TAccountCpiAuthority extends string,
  TAccountTaskRegistryProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RECEIPT_EMITTER_PROGRAM_ADDRESS,
>(
  input: EmitDelegatedReceiptInput<
    TAccountDelegate,
    TAccountIdentity,
    TAccountDelegation,
    TAccountTask,
    TAccountReceipt,
    TAccountDomainCatalog,
    TAccountCpiAuthority,
    TAccountTaskRegistryProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): EmitDelegatedReceiptInstruction<
  TProgramAddress,
  TAccountDelegate,
  TAccountIdentity,
  TAccountDelegation,
  TAccountTask,
  TAccountReceipt,
  TAccountDomainCatalog,
  TAccountCpiAuthority,
  TAccountTaskRegistryProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RECEIPT_EMITTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    delegate: { value: input.delegate ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    delegation: { value: input.delegation ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: true },
    receipt: { value: input.receipt ?? null, isWritable: true },
    domainCatalog: { value: input.domainCatalog ?? null, isWritable: false },
    cpiAuthority: { value: input.cpiAuthority ?? null, isWritable: false },
    taskRegistryProgram: {
      value: input.taskRegistryProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.taskRegistryProgram.value) {
    accounts.taskRegistryProgram.value =
      "5CjbVQQgjKeCqCsyxcb4HqPpAVgB8eNXZiZovaChQ7R4" as Address<"5CjbVQQgjKeCqCsyxcb4HqPpAVgB8eNXZiZovaChQ7R4">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("delegate", accounts.delegate),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("delegation", accounts.delegation),
      getAccountMeta("task", accounts.task),
      getAccountMeta("receipt", accounts.receipt),
      getAccountMeta("domainCatalog", accounts.domainCatalog),
      getAccountMeta("cpiAuthority", accounts.cpiAuthority),
      getAccountMeta("taskRegistryProgram", accounts.taskRegistryProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getEmitDelegatedReceiptInstructionDataEncoder().encode(
      args as EmitDelegatedReceiptInstructionDataArgs,
    ),
    programAddress,
  } as EmitDelegatedReceiptInstruction<
    TProgramAddress,
    TAccountDelegate,
    TAccountIdentity,
    TAccountDelegation,
    TAccountTask,
    TAccountReceipt,
    TAccountDomainCatalog,
    TAccountCpiAuthority,
    TAccountTaskRegistryProgram,
    TAccountSystemProgram
  >);
}

export type ParsedEmitDelegatedReceiptInstruction<
  TProgram extends string = typeof RECEIPT_EMITTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    delegate: TAccountMetas[0];
    identity: TAccountMetas[1];
    delegation: TAccountMetas[2];
    task: TAccountMetas[3];
    receipt: TAccountMetas[4];
    domainCatalog: TAccountMetas[5];
    cpiAuthority: TAccountMetas[6];
    taskRegistryProgram: TAccountMetas[7];
    systemProgram: TAccountMetas[8];
  };
  data: EmitDelegatedReceiptInstructionData;
};

export function parseEmitDelegatedReceiptInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedEmitDelegatedReceiptInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      delegate: getNextAccount(),
      identity: getNextAccount(),
      delegation: getNextAccount(),
      task: getNextAccount(),
      receipt: getNextAccount(),
      domainCatalog: getNextAccount(),
      cpiAuthority: getNextAccount(),
      taskRegistryProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getEmitDelegatedReceiptInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
