/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findCpiAuthorityPda } from "../pdas";
import { RECEIPT_EMITTER_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_CPI_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  34, 38, 210, 11, 10, 177, 205, 165,
]);

export function getInitializeCpiAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_CPI_AUTHORITY_DISCRIMINATOR,
  );
}

export type InitializeCpiAuthorityInstruction<
  TProgram extends string = typeof RECEIPT_EMITTER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountCpiAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountCpiAuthority extends string
        ? WritableAccount<TAccountCpiAuthority>
        : TAccountCpiAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeCpiAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeCpiAuthorityInstructionDataArgs = {};

export function getInitializeCpiAuthorityInstructionDataEncoder(): FixedSizeEncoder<InitializeCpiAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_CPI_AUTHORITY_DISCRIMINATOR,
    }),
  );
}

export function getInitializeCpiAuthorityInstructionDataDecoder(): FixedSizeDecoder<InitializeCpiAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeCpiAuthorityInstructionDataCodec(): FixedSizeCodec<
  InitializeCpiAuthorityInstructionDataArgs,
  InitializeCpiAuthorityInstructionData
> {
  return combineCodec(
    getInitializeCpiAuthorityInstructionDataEncoder(),
    getInitializeCpiAuthorityInstructionDataDecoder(),
  );
}

export type InitializeCpiAuthorityAsyncInput<
  TAccountPayer extends string = string,
  TAccountCpiAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  cpiAuthority?: Address<TAccountCpiAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeCpiAuthorityInstructionAsync<
  TAccountPayer extends string,
  TAccountCpiAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RECEIPT_EMITTER_PROGRAM_ADDRESS,
>(
  input: InitializeCpiAuthorityAsyncInput<
    TAccountPayer,
    TAccountCpiAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeCpiAuthorityInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountCpiAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RECEIPT_EMITTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    cpiAuthority: { value: input.cpiAuthority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.cpiAuthority.value) {
    accounts.cpiAuthority.value = await findCpiAuthorityPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("cpiAuthority", accounts.cpiAuthority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeCpiAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeCpiAuthorityInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountCpiAuthority,
    TAccountSystemProgram
  >);
}

export type InitializeCpiAuthorityInput<
  TAccountPayer extends string = string,
  TAccountCpiAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  cpiAuthority: Address<TAccountCpiAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeCpiAuthorityInstruction<
  TAccountPayer extends string,
  TAccountCpiAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof RECEIPT_EMITTER_PROGRAM_ADDRESS,
>(
  input: InitializeCpiAuthorityInput<
    TAccountPayer,
    TAccountCpiAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeCpiAuthorityInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountCpiAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RECEIPT_EMITTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    cpiAuthority: { value: input.cpiAuthority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("cpiAuthority", accounts.cpiAuthority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeCpiAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeCpiAuthorityInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountCpiAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeCpiAuthorityInstruction<
  TProgram extends string = typeof RECEIPT_EMITTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    cpiAuthority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeCpiAuthorityInstructionData;
};

export function parseInitializeCpiAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeCpiAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      cpiAuthority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeCpiAuthorityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
