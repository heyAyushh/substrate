/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithPayer,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/kit/program-client-core";
import {
  getCpiAuthorityCodec,
  getReceiptRecordCodec,
  type CpiAuthority,
  type CpiAuthorityArgs,
  type ReceiptRecord,
  type ReceiptRecordArgs,
} from "../accounts";
import {
  getEmitAuditReceiptInstructionAsync,
  getEmitChallengeResponseInstructionAsync,
  getEmitDelegatedReceiptInstructionAsync,
  getEmitHandoffGrantInstructionAsync,
  getEmitReceiptInstructionAsync,
  getFinalizeUnansweredChallengeInstructionAsync,
  getInitializeCpiAuthorityInstructionAsync,
  parseEmitAuditReceiptInstruction,
  parseEmitChallengeResponseInstruction,
  parseEmitDelegatedReceiptInstruction,
  parseEmitHandoffGrantInstruction,
  parseEmitReceiptInstruction,
  parseFinalizeUnansweredChallengeInstruction,
  parseInitializeCpiAuthorityInstruction,
  type EmitAuditReceiptAsyncInput,
  type EmitChallengeResponseAsyncInput,
  type EmitDelegatedReceiptAsyncInput,
  type EmitHandoffGrantAsyncInput,
  type EmitReceiptAsyncInput,
  type FinalizeUnansweredChallengeAsyncInput,
  type InitializeCpiAuthorityAsyncInput,
  type ParsedEmitAuditReceiptInstruction,
  type ParsedEmitChallengeResponseInstruction,
  type ParsedEmitDelegatedReceiptInstruction,
  type ParsedEmitHandoffGrantInstruction,
  type ParsedEmitReceiptInstruction,
  type ParsedFinalizeUnansweredChallengeInstruction,
  type ParsedInitializeCpiAuthorityInstruction,
} from "../instructions";
import {
  findAuditReceiptPda,
  findChallengeResponsePda,
  findCpiAuthorityPda,
  findReceiptPda,
} from "../pdas";

export const RECEIPT_EMITTER_PROGRAM_ADDRESS =
  "FV5Nsn3jHH8xxBP6m1N43NawgswmMkhZo72HGYJaJLHp" as Address<"FV5Nsn3jHH8xxBP6m1N43NawgswmMkhZo72HGYJaJLHp">;

export enum ReceiptEmitterAccount {
  CpiAuthority,
  ReceiptRecord,
}

export function identifyReceiptEmitterAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): ReceiptEmitterAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([16, 159, 44, 239, 207, 100, 75, 221]),
      ),
      0,
    )
  ) {
    return ReceiptEmitterAccount.CpiAuthority;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([51, 97, 207, 106, 28, 85, 70, 40]),
      ),
      0,
    )
  ) {
    return ReceiptEmitterAccount.ReceiptRecord;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "receiptEmitter" },
  );
}

export enum ReceiptEmitterInstruction {
  EmitAuditReceipt,
  EmitChallengeResponse,
  EmitDelegatedReceipt,
  EmitHandoffGrant,
  EmitReceipt,
  FinalizeUnansweredChallenge,
  InitializeCpiAuthority,
}

export function identifyReceiptEmitterInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): ReceiptEmitterInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([38, 161, 182, 96, 21, 214, 167, 157]),
      ),
      0,
    )
  ) {
    return ReceiptEmitterInstruction.EmitAuditReceipt;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([124, 138, 179, 95, 168, 8, 103, 166]),
      ),
      0,
    )
  ) {
    return ReceiptEmitterInstruction.EmitChallengeResponse;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([219, 75, 119, 117, 214, 32, 112, 4]),
      ),
      0,
    )
  ) {
    return ReceiptEmitterInstruction.EmitDelegatedReceipt;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([62, 80, 4, 222, 5, 37, 175, 127]),
      ),
      0,
    )
  ) {
    return ReceiptEmitterInstruction.EmitHandoffGrant;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([68, 22, 81, 246, 107, 232, 233, 9]),
      ),
      0,
    )
  ) {
    return ReceiptEmitterInstruction.EmitReceipt;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([59, 174, 55, 244, 91, 211, 90, 88]),
      ),
      0,
    )
  ) {
    return ReceiptEmitterInstruction.FinalizeUnansweredChallenge;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([34, 38, 210, 11, 10, 177, 205, 165]),
      ),
      0,
    )
  ) {
    return ReceiptEmitterInstruction.InitializeCpiAuthority;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "receiptEmitter" },
  );
}

export type ParsedReceiptEmitterInstruction<
  TProgram extends string = "FV5Nsn3jHH8xxBP6m1N43NawgswmMkhZo72HGYJaJLHp",
> =
  | ({
      instructionType: ReceiptEmitterInstruction.EmitAuditReceipt;
    } & ParsedEmitAuditReceiptInstruction<TProgram>)
  | ({
      instructionType: ReceiptEmitterInstruction.EmitChallengeResponse;
    } & ParsedEmitChallengeResponseInstruction<TProgram>)
  | ({
      instructionType: ReceiptEmitterInstruction.EmitDelegatedReceipt;
    } & ParsedEmitDelegatedReceiptInstruction<TProgram>)
  | ({
      instructionType: ReceiptEmitterInstruction.EmitHandoffGrant;
    } & ParsedEmitHandoffGrantInstruction<TProgram>)
  | ({
      instructionType: ReceiptEmitterInstruction.EmitReceipt;
    } & ParsedEmitReceiptInstruction<TProgram>)
  | ({
      instructionType: ReceiptEmitterInstruction.FinalizeUnansweredChallenge;
    } & ParsedFinalizeUnansweredChallengeInstruction<TProgram>)
  | ({
      instructionType: ReceiptEmitterInstruction.InitializeCpiAuthority;
    } & ParsedInitializeCpiAuthorityInstruction<TProgram>);

export function parseReceiptEmitterInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedReceiptEmitterInstruction<TProgram> {
  const instructionType = identifyReceiptEmitterInstruction(instruction);
  switch (instructionType) {
    case ReceiptEmitterInstruction.EmitAuditReceipt: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ReceiptEmitterInstruction.EmitAuditReceipt,
        ...parseEmitAuditReceiptInstruction(instruction),
      };
    }
    case ReceiptEmitterInstruction.EmitChallengeResponse: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ReceiptEmitterInstruction.EmitChallengeResponse,
        ...parseEmitChallengeResponseInstruction(instruction),
      };
    }
    case ReceiptEmitterInstruction.EmitDelegatedReceipt: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ReceiptEmitterInstruction.EmitDelegatedReceipt,
        ...parseEmitDelegatedReceiptInstruction(instruction),
      };
    }
    case ReceiptEmitterInstruction.EmitHandoffGrant: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ReceiptEmitterInstruction.EmitHandoffGrant,
        ...parseEmitHandoffGrantInstruction(instruction),
      };
    }
    case ReceiptEmitterInstruction.EmitReceipt: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ReceiptEmitterInstruction.EmitReceipt,
        ...parseEmitReceiptInstruction(instruction),
      };
    }
    case ReceiptEmitterInstruction.FinalizeUnansweredChallenge: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ReceiptEmitterInstruction.FinalizeUnansweredChallenge,
        ...parseFinalizeUnansweredChallengeInstruction(instruction),
      };
    }
    case ReceiptEmitterInstruction.InitializeCpiAuthority: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ReceiptEmitterInstruction.InitializeCpiAuthority,
        ...parseInitializeCpiAuthorityInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "receiptEmitter",
        },
      );
  }
}

export type ReceiptEmitterPlugin = {
  accounts: ReceiptEmitterPluginAccounts;
  instructions: ReceiptEmitterPluginInstructions;
  pdas: ReceiptEmitterPluginPdas;
};

export type ReceiptEmitterPluginAccounts = {
  cpiAuthority: ReturnType<typeof getCpiAuthorityCodec> &
    SelfFetchFunctions<CpiAuthorityArgs, CpiAuthority>;
  receiptRecord: ReturnType<typeof getReceiptRecordCodec> &
    SelfFetchFunctions<ReceiptRecordArgs, ReceiptRecord>;
};

export type ReceiptEmitterPluginInstructions = {
  emitAuditReceipt: (
    input: EmitAuditReceiptAsyncInput,
  ) => ReturnType<typeof getEmitAuditReceiptInstructionAsync> &
    SelfPlanAndSendFunctions;
  emitChallengeResponse: (
    input: EmitChallengeResponseAsyncInput,
  ) => ReturnType<typeof getEmitChallengeResponseInstructionAsync> &
    SelfPlanAndSendFunctions;
  emitDelegatedReceipt: (
    input: EmitDelegatedReceiptAsyncInput,
  ) => ReturnType<typeof getEmitDelegatedReceiptInstructionAsync> &
    SelfPlanAndSendFunctions;
  emitHandoffGrant: (
    input: EmitHandoffGrantAsyncInput,
  ) => ReturnType<typeof getEmitHandoffGrantInstructionAsync> &
    SelfPlanAndSendFunctions;
  emitReceipt: (
    input: EmitReceiptAsyncInput,
  ) => ReturnType<typeof getEmitReceiptInstructionAsync> &
    SelfPlanAndSendFunctions;
  finalizeUnansweredChallenge: (
    input: FinalizeUnansweredChallengeAsyncInput,
  ) => ReturnType<typeof getFinalizeUnansweredChallengeInstructionAsync> &
    SelfPlanAndSendFunctions;
  initializeCpiAuthority: (
    input: MakeOptional<InitializeCpiAuthorityAsyncInput, "payer">,
  ) => ReturnType<typeof getInitializeCpiAuthorityInstructionAsync> &
    SelfPlanAndSendFunctions;
};

export type ReceiptEmitterPluginPdas = {
  auditReceipt: typeof findAuditReceiptPda;
  cpiAuthority: typeof findCpiAuthorityPda;
  challengeResponse: typeof findChallengeResponsePda;
  receipt: typeof findReceiptPda;
};

export type ReceiptEmitterPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithPayer &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function receiptEmitterProgram() {
  return <T extends ReceiptEmitterPluginRequirements>(client: T) => {
    return {
      ...client,
      receiptEmitter: <ReceiptEmitterPlugin>{
        accounts: {
          cpiAuthority: addSelfFetchFunctions(client, getCpiAuthorityCodec()),
          receiptRecord: addSelfFetchFunctions(client, getReceiptRecordCodec()),
        },
        instructions: {
          emitAuditReceipt: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getEmitAuditReceiptInstructionAsync(input),
            ),
          emitChallengeResponse: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getEmitChallengeResponseInstructionAsync(input),
            ),
          emitDelegatedReceipt: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getEmitDelegatedReceiptInstructionAsync(input),
            ),
          emitHandoffGrant: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getEmitHandoffGrantInstructionAsync(input),
            ),
          emitReceipt: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getEmitReceiptInstructionAsync(input),
            ),
          finalizeUnansweredChallenge: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getFinalizeUnansweredChallengeInstructionAsync(input),
            ),
          initializeCpiAuthority: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeCpiAuthorityInstructionAsync({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
        },
        pdas: {
          auditReceipt: findAuditReceiptPda,
          cpiAuthority: findCpiAuthorityPda,
          challengeResponse: findChallengeResponsePda,
          receipt: findReceiptPda,
        },
      },
    };
  };
}

type MakeOptional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;
