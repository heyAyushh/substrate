/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findReceiptApplicationPda } from "../pdas";
import { REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS } from "../programs";

export const APPLY_REPUTATION_RECEIPT_DISCRIMINATOR = new Uint8Array([
  72, 116, 117, 91, 41, 221, 149, 2,
]);

export function getApplyReputationReceiptDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPLY_REPUTATION_RECEIPT_DISCRIMINATOR,
  );
}

export type ApplyReputationReceiptInstruction<
  TProgram extends string = typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountReceipt extends string | AccountMeta<string> = string,
  TAccountReputation extends string | AccountMeta<string> = string,
  TAccountReceiptApplication extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIdentity extends string
        ? ReadonlyAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountReceipt extends string
        ? ReadonlyAccount<TAccountReceipt>
        : TAccountReceipt,
      TAccountReputation extends string
        ? WritableAccount<TAccountReputation>
        : TAccountReputation,
      TAccountReceiptApplication extends string
        ? WritableAccount<TAccountReceiptApplication>
        : TAccountReceiptApplication,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ApplyReputationReceiptInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ApplyReputationReceiptInstructionDataArgs = {};

export function getApplyReputationReceiptInstructionDataEncoder(): FixedSizeEncoder<ApplyReputationReceiptInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: APPLY_REPUTATION_RECEIPT_DISCRIMINATOR,
    }),
  );
}

export function getApplyReputationReceiptInstructionDataDecoder(): FixedSizeDecoder<ApplyReputationReceiptInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApplyReputationReceiptInstructionDataCodec(): FixedSizeCodec<
  ApplyReputationReceiptInstructionDataArgs,
  ApplyReputationReceiptInstructionData
> {
  return combineCodec(
    getApplyReputationReceiptInstructionDataEncoder(),
    getApplyReputationReceiptInstructionDataDecoder(),
  );
}

export type ApplyReputationReceiptAsyncInput<
  TAccountIdentity extends string = string,
  TAccountAuthority extends string = string,
  TAccountReceipt extends string = string,
  TAccountReputation extends string = string,
  TAccountReceiptApplication extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  identity: Address<TAccountIdentity>;
  authority: TransactionSigner<TAccountAuthority>;
  receipt: Address<TAccountReceipt>;
  reputation: Address<TAccountReputation>;
  receiptApplication?: Address<TAccountReceiptApplication>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getApplyReputationReceiptInstructionAsync<
  TAccountIdentity extends string,
  TAccountAuthority extends string,
  TAccountReceipt extends string,
  TAccountReputation extends string,
  TAccountReceiptApplication extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address =
    typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
>(
  input: ApplyReputationReceiptAsyncInput<
    TAccountIdentity,
    TAccountAuthority,
    TAccountReceipt,
    TAccountReputation,
    TAccountReceiptApplication,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ApplyReputationReceiptInstruction<
    TProgramAddress,
    TAccountIdentity,
    TAccountAuthority,
    TAccountReceipt,
    TAccountReputation,
    TAccountReceiptApplication,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    identity: { value: input.identity ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    receipt: { value: input.receipt ?? null, isWritable: false },
    reputation: { value: input.reputation ?? null, isWritable: true },
    receiptApplication: {
      value: input.receiptApplication ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.receiptApplication.value) {
    accounts.receiptApplication.value = await findReceiptApplicationPda({
      reputation: getAddressFromResolvedInstructionAccount(
        "reputation",
        accounts.reputation.value,
      ),
      receipt: getAddressFromResolvedInstructionAccount(
        "receipt",
        accounts.receipt.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("receipt", accounts.receipt),
      getAccountMeta("reputation", accounts.reputation),
      getAccountMeta("receiptApplication", accounts.receiptApplication),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getApplyReputationReceiptInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyReputationReceiptInstruction<
    TProgramAddress,
    TAccountIdentity,
    TAccountAuthority,
    TAccountReceipt,
    TAccountReputation,
    TAccountReceiptApplication,
    TAccountSystemProgram
  >);
}

export type ApplyReputationReceiptInput<
  TAccountIdentity extends string = string,
  TAccountAuthority extends string = string,
  TAccountReceipt extends string = string,
  TAccountReputation extends string = string,
  TAccountReceiptApplication extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  identity: Address<TAccountIdentity>;
  authority: TransactionSigner<TAccountAuthority>;
  receipt: Address<TAccountReceipt>;
  reputation: Address<TAccountReputation>;
  receiptApplication: Address<TAccountReceiptApplication>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getApplyReputationReceiptInstruction<
  TAccountIdentity extends string,
  TAccountAuthority extends string,
  TAccountReceipt extends string,
  TAccountReputation extends string,
  TAccountReceiptApplication extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address =
    typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
>(
  input: ApplyReputationReceiptInput<
    TAccountIdentity,
    TAccountAuthority,
    TAccountReceipt,
    TAccountReputation,
    TAccountReceiptApplication,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ApplyReputationReceiptInstruction<
  TProgramAddress,
  TAccountIdentity,
  TAccountAuthority,
  TAccountReceipt,
  TAccountReputation,
  TAccountReceiptApplication,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    identity: { value: input.identity ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    receipt: { value: input.receipt ?? null, isWritable: false },
    reputation: { value: input.reputation ?? null, isWritable: true },
    receiptApplication: {
      value: input.receiptApplication ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("receipt", accounts.receipt),
      getAccountMeta("reputation", accounts.reputation),
      getAccountMeta("receiptApplication", accounts.receiptApplication),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getApplyReputationReceiptInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyReputationReceiptInstruction<
    TProgramAddress,
    TAccountIdentity,
    TAccountAuthority,
    TAccountReceipt,
    TAccountReputation,
    TAccountReceiptApplication,
    TAccountSystemProgram
  >);
}

export type ParsedApplyReputationReceiptInstruction<
  TProgram extends string = typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    identity: TAccountMetas[0];
    authority: TAccountMetas[1];
    receipt: TAccountMetas[2];
    reputation: TAccountMetas[3];
    receiptApplication: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: ApplyReputationReceiptInstructionData;
};

export function parseApplyReputationReceiptInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApplyReputationReceiptInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      identity: getNextAccount(),
      authority: getNextAccount(),
      receipt: getNextAccount(),
      reputation: getNextAccount(),
      receiptApplication: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getApplyReputationReceiptInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
