/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findDomainCatalogPda } from "../pdas";
import { REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS } from "../programs";

export const DEPRECATE_DOMAIN_DISCRIMINATOR = new Uint8Array([
  76, 17, 149, 208, 83, 202, 60, 85,
]);

export function getDeprecateDomainDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEPRECATE_DOMAIN_DISCRIMINATOR,
  );
}

export type DeprecateDomainInstruction<
  TProgram extends string = typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
  TAccountCurator extends string | AccountMeta<string> = string,
  TAccountDomainCatalog extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCurator extends string
        ? WritableSignerAccount<TAccountCurator> &
            AccountSignerMeta<TAccountCurator>
        : TAccountCurator,
      TAccountDomainCatalog extends string
        ? WritableAccount<TAccountDomainCatalog>
        : TAccountDomainCatalog,
      ...TRemainingAccounts,
    ]
  >;

export type DeprecateDomainInstructionData = {
  discriminator: ReadonlyUint8Array;
  domain: ReadonlyUint8Array;
};

export type DeprecateDomainInstructionDataArgs = { domain: ReadonlyUint8Array };

export function getDeprecateDomainInstructionDataEncoder(): FixedSizeEncoder<DeprecateDomainInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["domain", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: DEPRECATE_DOMAIN_DISCRIMINATOR }),
  );
}

export function getDeprecateDomainInstructionDataDecoder(): FixedSizeDecoder<DeprecateDomainInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["domain", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getDeprecateDomainInstructionDataCodec(): FixedSizeCodec<
  DeprecateDomainInstructionDataArgs,
  DeprecateDomainInstructionData
> {
  return combineCodec(
    getDeprecateDomainInstructionDataEncoder(),
    getDeprecateDomainInstructionDataDecoder(),
  );
}

export type DeprecateDomainAsyncInput<
  TAccountCurator extends string = string,
  TAccountDomainCatalog extends string = string,
> = {
  curator: TransactionSigner<TAccountCurator>;
  domainCatalog?: Address<TAccountDomainCatalog>;
  domain: DeprecateDomainInstructionDataArgs["domain"];
};

export async function getDeprecateDomainInstructionAsync<
  TAccountCurator extends string,
  TAccountDomainCatalog extends string,
  TProgramAddress extends Address =
    typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
>(
  input: DeprecateDomainAsyncInput<TAccountCurator, TAccountDomainCatalog>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DeprecateDomainInstruction<
    TProgramAddress,
    TAccountCurator,
    TAccountDomainCatalog
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    curator: { value: input.curator ?? null, isWritable: true },
    domainCatalog: { value: input.domainCatalog ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.domainCatalog.value) {
    accounts.domainCatalog.value = await findDomainCatalogPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("curator", accounts.curator),
      getAccountMeta("domainCatalog", accounts.domainCatalog),
    ],
    data: getDeprecateDomainInstructionDataEncoder().encode(
      args as DeprecateDomainInstructionDataArgs,
    ),
    programAddress,
  } as DeprecateDomainInstruction<
    TProgramAddress,
    TAccountCurator,
    TAccountDomainCatalog
  >);
}

export type DeprecateDomainInput<
  TAccountCurator extends string = string,
  TAccountDomainCatalog extends string = string,
> = {
  curator: TransactionSigner<TAccountCurator>;
  domainCatalog: Address<TAccountDomainCatalog>;
  domain: DeprecateDomainInstructionDataArgs["domain"];
};

export function getDeprecateDomainInstruction<
  TAccountCurator extends string,
  TAccountDomainCatalog extends string,
  TProgramAddress extends Address =
    typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
>(
  input: DeprecateDomainInput<TAccountCurator, TAccountDomainCatalog>,
  config?: { programAddress?: TProgramAddress },
): DeprecateDomainInstruction<
  TProgramAddress,
  TAccountCurator,
  TAccountDomainCatalog
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    curator: { value: input.curator ?? null, isWritable: true },
    domainCatalog: { value: input.domainCatalog ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("curator", accounts.curator),
      getAccountMeta("domainCatalog", accounts.domainCatalog),
    ],
    data: getDeprecateDomainInstructionDataEncoder().encode(
      args as DeprecateDomainInstructionDataArgs,
    ),
    programAddress,
  } as DeprecateDomainInstruction<
    TProgramAddress,
    TAccountCurator,
    TAccountDomainCatalog
  >);
}

export type ParsedDeprecateDomainInstruction<
  TProgram extends string = typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    curator: TAccountMetas[0];
    domainCatalog: TAccountMetas[1];
  };
  data: DeprecateDomainInstructionData;
};

export function parseDeprecateDomainInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDeprecateDomainInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { curator: getNextAccount(), domainCatalog: getNextAccount() },
    data: getDeprecateDomainInstructionDataDecoder().decode(instruction.data),
  };
}
