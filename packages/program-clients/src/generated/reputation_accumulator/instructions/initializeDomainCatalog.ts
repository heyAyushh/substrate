/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findDomainCatalogPda } from "../pdas";
import { REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_DOMAIN_CATALOG_DISCRIMINATOR = new Uint8Array([
  236, 208, 168, 194, 2, 209, 138, 178,
]);

export function getInitializeDomainCatalogDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_DOMAIN_CATALOG_DISCRIMINATOR,
  );
}

export type InitializeDomainCatalogInstruction<
  TProgram extends string = typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
  TAccountCurator extends string | AccountMeta<string> = string,
  TAccountDomainCatalog extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCurator extends string
        ? WritableSignerAccount<TAccountCurator> &
            AccountSignerMeta<TAccountCurator>
        : TAccountCurator,
      TAccountDomainCatalog extends string
        ? WritableAccount<TAccountDomainCatalog>
        : TAccountDomainCatalog,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeDomainCatalogInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeDomainCatalogInstructionDataArgs = {};

export function getInitializeDomainCatalogInstructionDataEncoder(): FixedSizeEncoder<InitializeDomainCatalogInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_DOMAIN_CATALOG_DISCRIMINATOR,
    }),
  );
}

export function getInitializeDomainCatalogInstructionDataDecoder(): FixedSizeDecoder<InitializeDomainCatalogInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeDomainCatalogInstructionDataCodec(): FixedSizeCodec<
  InitializeDomainCatalogInstructionDataArgs,
  InitializeDomainCatalogInstructionData
> {
  return combineCodec(
    getInitializeDomainCatalogInstructionDataEncoder(),
    getInitializeDomainCatalogInstructionDataDecoder(),
  );
}

export type InitializeDomainCatalogAsyncInput<
  TAccountCurator extends string = string,
  TAccountDomainCatalog extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  curator: TransactionSigner<TAccountCurator>;
  domainCatalog?: Address<TAccountDomainCatalog>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeDomainCatalogInstructionAsync<
  TAccountCurator extends string,
  TAccountDomainCatalog extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address =
    typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
>(
  input: InitializeDomainCatalogAsyncInput<
    TAccountCurator,
    TAccountDomainCatalog,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeDomainCatalogInstruction<
    TProgramAddress,
    TAccountCurator,
    TAccountDomainCatalog,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    curator: { value: input.curator ?? null, isWritable: true },
    domainCatalog: { value: input.domainCatalog ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.domainCatalog.value) {
    accounts.domainCatalog.value = await findDomainCatalogPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("curator", accounts.curator),
      getAccountMeta("domainCatalog", accounts.domainCatalog),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeDomainCatalogInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeDomainCatalogInstruction<
    TProgramAddress,
    TAccountCurator,
    TAccountDomainCatalog,
    TAccountSystemProgram
  >);
}

export type InitializeDomainCatalogInput<
  TAccountCurator extends string = string,
  TAccountDomainCatalog extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  curator: TransactionSigner<TAccountCurator>;
  domainCatalog: Address<TAccountDomainCatalog>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeDomainCatalogInstruction<
  TAccountCurator extends string,
  TAccountDomainCatalog extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address =
    typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
>(
  input: InitializeDomainCatalogInput<
    TAccountCurator,
    TAccountDomainCatalog,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeDomainCatalogInstruction<
  TProgramAddress,
  TAccountCurator,
  TAccountDomainCatalog,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    curator: { value: input.curator ?? null, isWritable: true },
    domainCatalog: { value: input.domainCatalog ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("curator", accounts.curator),
      getAccountMeta("domainCatalog", accounts.domainCatalog),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeDomainCatalogInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeDomainCatalogInstruction<
    TProgramAddress,
    TAccountCurator,
    TAccountDomainCatalog,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeDomainCatalogInstruction<
  TProgram extends string = typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    curator: TAccountMetas[0];
    domainCatalog: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeDomainCatalogInstructionData;
};

export function parseInitializeDomainCatalogInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeDomainCatalogInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      curator: getNextAccount(),
      domainCatalog: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeDomainCatalogInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
