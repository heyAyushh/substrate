/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findReceiptApplicationPda } from "../pdas";
import { REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS } from "../programs";

export const REPUTATION_RECEIPT_ALREADY_APPLIED_DISCRIMINATOR = new Uint8Array([
  93, 137, 21, 93, 141, 12, 67, 54,
]);

export function getReputationReceiptAlreadyAppliedDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REPUTATION_RECEIPT_ALREADY_APPLIED_DISCRIMINATOR,
  );
}

export type ReputationReceiptAlreadyAppliedInstruction<
  TProgram extends string = typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountReceipt extends string | AccountMeta<string> = string,
  TAccountReputation extends string | AccountMeta<string> = string,
  TAccountReceiptApplication extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountIdentity extends string
        ? ReadonlyAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountReceipt extends string
        ? ReadonlyAccount<TAccountReceipt>
        : TAccountReceipt,
      TAccountReputation extends string
        ? ReadonlyAccount<TAccountReputation>
        : TAccountReputation,
      TAccountReceiptApplication extends string
        ? ReadonlyAccount<TAccountReceiptApplication>
        : TAccountReceiptApplication,
      ...TRemainingAccounts,
    ]
  >;

export type ReputationReceiptAlreadyAppliedInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ReputationReceiptAlreadyAppliedInstructionDataArgs = {};

export function getReputationReceiptAlreadyAppliedInstructionDataEncoder(): FixedSizeEncoder<ReputationReceiptAlreadyAppliedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: REPUTATION_RECEIPT_ALREADY_APPLIED_DISCRIMINATOR,
    }),
  );
}

export function getReputationReceiptAlreadyAppliedInstructionDataDecoder(): FixedSizeDecoder<ReputationReceiptAlreadyAppliedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getReputationReceiptAlreadyAppliedInstructionDataCodec(): FixedSizeCodec<
  ReputationReceiptAlreadyAppliedInstructionDataArgs,
  ReputationReceiptAlreadyAppliedInstructionData
> {
  return combineCodec(
    getReputationReceiptAlreadyAppliedInstructionDataEncoder(),
    getReputationReceiptAlreadyAppliedInstructionDataDecoder(),
  );
}

export type ReputationReceiptAlreadyAppliedAsyncInput<
  TAccountIdentity extends string = string,
  TAccountAuthority extends string = string,
  TAccountReceipt extends string = string,
  TAccountReputation extends string = string,
  TAccountReceiptApplication extends string = string,
> = {
  identity: Address<TAccountIdentity>;
  authority: TransactionSigner<TAccountAuthority>;
  receipt: Address<TAccountReceipt>;
  reputation: Address<TAccountReputation>;
  receiptApplication?: Address<TAccountReceiptApplication>;
};

export async function getReputationReceiptAlreadyAppliedInstructionAsync<
  TAccountIdentity extends string,
  TAccountAuthority extends string,
  TAccountReceipt extends string,
  TAccountReputation extends string,
  TAccountReceiptApplication extends string,
  TProgramAddress extends Address =
    typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
>(
  input: ReputationReceiptAlreadyAppliedAsyncInput<
    TAccountIdentity,
    TAccountAuthority,
    TAccountReceipt,
    TAccountReputation,
    TAccountReceiptApplication
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ReputationReceiptAlreadyAppliedInstruction<
    TProgramAddress,
    TAccountIdentity,
    TAccountAuthority,
    TAccountReceipt,
    TAccountReputation,
    TAccountReceiptApplication
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    identity: { value: input.identity ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    receipt: { value: input.receipt ?? null, isWritable: false },
    reputation: { value: input.reputation ?? null, isWritable: false },
    receiptApplication: {
      value: input.receiptApplication ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.receiptApplication.value) {
    accounts.receiptApplication.value = await findReceiptApplicationPda({
      reputation: getAddressFromResolvedInstructionAccount(
        "reputation",
        accounts.reputation.value,
      ),
      receipt: getAddressFromResolvedInstructionAccount(
        "receipt",
        accounts.receipt.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("receipt", accounts.receipt),
      getAccountMeta("reputation", accounts.reputation),
      getAccountMeta("receiptApplication", accounts.receiptApplication),
    ],
    data: getReputationReceiptAlreadyAppliedInstructionDataEncoder().encode({}),
    programAddress,
  } as ReputationReceiptAlreadyAppliedInstruction<
    TProgramAddress,
    TAccountIdentity,
    TAccountAuthority,
    TAccountReceipt,
    TAccountReputation,
    TAccountReceiptApplication
  >);
}

export type ReputationReceiptAlreadyAppliedInput<
  TAccountIdentity extends string = string,
  TAccountAuthority extends string = string,
  TAccountReceipt extends string = string,
  TAccountReputation extends string = string,
  TAccountReceiptApplication extends string = string,
> = {
  identity: Address<TAccountIdentity>;
  authority: TransactionSigner<TAccountAuthority>;
  receipt: Address<TAccountReceipt>;
  reputation: Address<TAccountReputation>;
  receiptApplication: Address<TAccountReceiptApplication>;
};

export function getReputationReceiptAlreadyAppliedInstruction<
  TAccountIdentity extends string,
  TAccountAuthority extends string,
  TAccountReceipt extends string,
  TAccountReputation extends string,
  TAccountReceiptApplication extends string,
  TProgramAddress extends Address =
    typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
>(
  input: ReputationReceiptAlreadyAppliedInput<
    TAccountIdentity,
    TAccountAuthority,
    TAccountReceipt,
    TAccountReputation,
    TAccountReceiptApplication
  >,
  config?: { programAddress?: TProgramAddress },
): ReputationReceiptAlreadyAppliedInstruction<
  TProgramAddress,
  TAccountIdentity,
  TAccountAuthority,
  TAccountReceipt,
  TAccountReputation,
  TAccountReceiptApplication
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    identity: { value: input.identity ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    receipt: { value: input.receipt ?? null, isWritable: false },
    reputation: { value: input.reputation ?? null, isWritable: false },
    receiptApplication: {
      value: input.receiptApplication ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("receipt", accounts.receipt),
      getAccountMeta("reputation", accounts.reputation),
      getAccountMeta("receiptApplication", accounts.receiptApplication),
    ],
    data: getReputationReceiptAlreadyAppliedInstructionDataEncoder().encode({}),
    programAddress,
  } as ReputationReceiptAlreadyAppliedInstruction<
    TProgramAddress,
    TAccountIdentity,
    TAccountAuthority,
    TAccountReceipt,
    TAccountReputation,
    TAccountReceiptApplication
  >);
}

export type ParsedReputationReceiptAlreadyAppliedInstruction<
  TProgram extends string = typeof REPUTATION_ACCUMULATOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    identity: TAccountMetas[0];
    authority: TAccountMetas[1];
    receipt: TAccountMetas[2];
    reputation: TAccountMetas[3];
    receiptApplication: TAccountMetas[4];
  };
  data: ReputationReceiptAlreadyAppliedInstructionData;
};

export function parseReputationReceiptAlreadyAppliedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReputationReceiptAlreadyAppliedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      identity: getNextAccount(),
      authority: getNextAccount(),
      receipt: getNextAccount(),
      reputation: getNextAccount(),
      receiptApplication: getNextAccount(),
    },
    data: getReputationReceiptAlreadyAppliedInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
