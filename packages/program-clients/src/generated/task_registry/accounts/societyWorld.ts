/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SOCIETY_WORLD_DISCRIMINATOR = new Uint8Array([
  49, 146, 44, 40, 130, 73, 254, 204,
]);

export function getSocietyWorldDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SOCIETY_WORLD_DISCRIMINATOR,
  );
}

export type SocietyWorld = {
  discriminator: ReadonlyUint8Array;
  identity: Address;
  task: Address;
  currentTick: number;
  lastSequence: bigint;
  lastReceipt: Address;
  stateHash: ReadonlyUint8Array;
  status: number;
  state: ReadonlyUint8Array;
  bump: number;
};

export type SocietyWorldArgs = {
  identity: Address;
  task: Address;
  currentTick: number;
  lastSequence: number | bigint;
  lastReceipt: Address;
  stateHash: ReadonlyUint8Array;
  status: number;
  state: ReadonlyUint8Array;
  bump: number;
};

/** Gets the encoder for {@link SocietyWorldArgs} account data. */
export function getSocietyWorldEncoder(): Encoder<SocietyWorldArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["identity", getAddressEncoder()],
      ["task", getAddressEncoder()],
      ["currentTick", getU32Encoder()],
      ["lastSequence", getU64Encoder()],
      ["lastReceipt", getAddressEncoder()],
      ["stateHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["status", getU8Encoder()],
      ["state", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SOCIETY_WORLD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link SocietyWorld} account data. */
export function getSocietyWorldDecoder(): Decoder<SocietyWorld> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["identity", getAddressDecoder()],
    ["task", getAddressDecoder()],
    ["currentTick", getU32Decoder()],
    ["lastSequence", getU64Decoder()],
    ["lastReceipt", getAddressDecoder()],
    ["stateHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["status", getU8Decoder()],
    ["state", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link SocietyWorld} account data. */
export function getSocietyWorldCodec(): Codec<SocietyWorldArgs, SocietyWorld> {
  return combineCodec(getSocietyWorldEncoder(), getSocietyWorldDecoder());
}

export function decodeSocietyWorld<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<SocietyWorld, TAddress>;
export function decodeSocietyWorld<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<SocietyWorld, TAddress>;
export function decodeSocietyWorld<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<SocietyWorld, TAddress> | MaybeAccount<SocietyWorld, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSocietyWorldDecoder(),
  );
}

export async function fetchSocietyWorld<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<SocietyWorld, TAddress>> {
  const maybeAccount = await fetchMaybeSocietyWorld(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSocietyWorld<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<SocietyWorld, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSocietyWorld(maybeAccount);
}

export async function fetchAllSocietyWorld(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<SocietyWorld>[]> {
  const maybeAccounts = await fetchAllMaybeSocietyWorld(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSocietyWorld(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<SocietyWorld>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSocietyWorld(maybeAccount));
}
