/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { TASK_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const ADVANCE_RECEIPT_CHAIN_DISCRIMINATOR = new Uint8Array([
  149, 172, 149, 216, 210, 77, 207, 37,
]);

export function getAdvanceReceiptChainDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADVANCE_RECEIPT_CHAIN_DISCRIMINATOR,
  );
}

export type AdvanceReceiptChainInstruction<
  TProgram extends string = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountIdentity extends string
        ? ReadonlyAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type AdvanceReceiptChainInstructionData = {
  discriminator: ReadonlyUint8Array;
  lastReceipt: Address;
  lastSequence: bigint;
};

export type AdvanceReceiptChainInstructionDataArgs = {
  lastReceipt: Address;
  lastSequence: number | bigint;
};

export function getAdvanceReceiptChainInstructionDataEncoder(): FixedSizeEncoder<AdvanceReceiptChainInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["lastReceipt", getAddressEncoder()],
      ["lastSequence", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: ADVANCE_RECEIPT_CHAIN_DISCRIMINATOR,
    }),
  );
}

export function getAdvanceReceiptChainInstructionDataDecoder(): FixedSizeDecoder<AdvanceReceiptChainInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["lastReceipt", getAddressDecoder()],
    ["lastSequence", getU64Decoder()],
  ]);
}

export function getAdvanceReceiptChainInstructionDataCodec(): FixedSizeCodec<
  AdvanceReceiptChainInstructionDataArgs,
  AdvanceReceiptChainInstructionData
> {
  return combineCodec(
    getAdvanceReceiptChainInstructionDataEncoder(),
    getAdvanceReceiptChainInstructionDataDecoder(),
  );
}

export type AdvanceReceiptChainAsyncInput<
  TAccountTask extends string = string,
  TAccountIdentity extends string = string,
  TAccountAuthority extends string = string,
> = {
  task: Address<TAccountTask>;
  identity: Address<TAccountIdentity>;
  authority?: TransactionSigner<TAccountAuthority>;
  lastReceipt: AdvanceReceiptChainInstructionDataArgs["lastReceipt"];
  lastSequence: AdvanceReceiptChainInstructionDataArgs["lastSequence"];
};

export async function getAdvanceReceiptChainInstructionAsync<
  TAccountTask extends string,
  TAccountIdentity extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AdvanceReceiptChainAsyncInput<
    TAccountTask,
    TAccountIdentity,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdvanceReceiptChainInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountIdentity,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TASK_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.authority.value) {
    accounts.authority.value = await getProgramDerivedAddress({
      programAddress:
        "FR2iXdHVBWbzkdn5qQdWEuyLWWaB2zR9ipRLTA8rGvJk" as Address<"FR2iXdHVBWbzkdn5qQdWEuyLWWaB2zR9ipRLTA8rGvJk">,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            99, 112, 105, 95, 97, 117, 116, 104, 111, 114, 105, 116, 121,
          ]),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getAdvanceReceiptChainInstructionDataEncoder().encode(
      args as AdvanceReceiptChainInstructionDataArgs,
    ),
    programAddress,
  } as AdvanceReceiptChainInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountIdentity,
    TAccountAuthority
  >);
}

export type AdvanceReceiptChainInput<
  TAccountTask extends string = string,
  TAccountIdentity extends string = string,
  TAccountAuthority extends string = string,
> = {
  task: Address<TAccountTask>;
  identity: Address<TAccountIdentity>;
  authority: TransactionSigner<TAccountAuthority>;
  lastReceipt: AdvanceReceiptChainInstructionDataArgs["lastReceipt"];
  lastSequence: AdvanceReceiptChainInstructionDataArgs["lastSequence"];
};

export function getAdvanceReceiptChainInstruction<
  TAccountTask extends string,
  TAccountIdentity extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AdvanceReceiptChainInput<
    TAccountTask,
    TAccountIdentity,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): AdvanceReceiptChainInstruction<
  TProgramAddress,
  TAccountTask,
  TAccountIdentity,
  TAccountAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TASK_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    task: { value: input.task ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("task", accounts.task),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getAdvanceReceiptChainInstructionDataEncoder().encode(
      args as AdvanceReceiptChainInstructionDataArgs,
    ),
    programAddress,
  } as AdvanceReceiptChainInstruction<
    TProgramAddress,
    TAccountTask,
    TAccountIdentity,
    TAccountAuthority
  >);
}

export type ParsedAdvanceReceiptChainInstruction<
  TProgram extends string = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    task: TAccountMetas[0];
    identity: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: AdvanceReceiptChainInstructionData;
};

export function parseAdvanceReceiptChainInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdvanceReceiptChainInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      task: getNextAccount(),
      identity: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getAdvanceReceiptChainInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
