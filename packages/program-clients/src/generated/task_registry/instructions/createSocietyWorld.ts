/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findSocietyWorldPda } from "../pdas";
import { TASK_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const CREATE_SOCIETY_WORLD_DISCRIMINATOR = new Uint8Array([
  93, 92, 70, 129, 116, 174, 88, 197,
]);

export function getCreateSocietyWorldDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_SOCIETY_WORLD_DISCRIMINATOR,
  );
}

export type CreateSocietyWorldInstruction<
  TProgram extends string = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountSocietyWorld extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? ReadonlyAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountTask extends string
        ? ReadonlyAccount<TAccountTask>
        : TAccountTask,
      TAccountSocietyWorld extends string
        ? WritableAccount<TAccountSocietyWorld>
        : TAccountSocietyWorld,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateSocietyWorldInstructionData = {
  discriminator: ReadonlyUint8Array;
  currentTick: number;
  lastSequence: bigint;
  lastReceipt: Address;
  status: number;
  state: ReadonlyUint8Array;
};

export type CreateSocietyWorldInstructionDataArgs = {
  currentTick: number;
  lastSequence: number | bigint;
  lastReceipt: Address;
  status: number;
  state: ReadonlyUint8Array;
};

export function getCreateSocietyWorldInstructionDataEncoder(): Encoder<CreateSocietyWorldInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["currentTick", getU32Encoder()],
      ["lastSequence", getU64Encoder()],
      ["lastReceipt", getAddressEncoder()],
      ["status", getU8Encoder()],
      ["state", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: CREATE_SOCIETY_WORLD_DISCRIMINATOR,
    }),
  );
}

export function getCreateSocietyWorldInstructionDataDecoder(): Decoder<CreateSocietyWorldInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["currentTick", getU32Decoder()],
    ["lastSequence", getU64Decoder()],
    ["lastReceipt", getAddressDecoder()],
    ["status", getU8Decoder()],
    ["state", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getCreateSocietyWorldInstructionDataCodec(): Codec<
  CreateSocietyWorldInstructionDataArgs,
  CreateSocietyWorldInstructionData
> {
  return combineCodec(
    getCreateSocietyWorldInstructionDataEncoder(),
    getCreateSocietyWorldInstructionDataDecoder(),
  );
}

export type CreateSocietyWorldAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountTask extends string = string,
  TAccountSocietyWorld extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  task: Address<TAccountTask>;
  societyWorld?: Address<TAccountSocietyWorld>;
  systemProgram?: Address<TAccountSystemProgram>;
  currentTick: CreateSocietyWorldInstructionDataArgs["currentTick"];
  lastSequence: CreateSocietyWorldInstructionDataArgs["lastSequence"];
  lastReceipt: CreateSocietyWorldInstructionDataArgs["lastReceipt"];
  status: CreateSocietyWorldInstructionDataArgs["status"];
  state: CreateSocietyWorldInstructionDataArgs["state"];
};

export async function getCreateSocietyWorldInstructionAsync<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountTask extends string,
  TAccountSocietyWorld extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CreateSocietyWorldAsyncInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountSocietyWorld,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateSocietyWorldInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountSocietyWorld,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TASK_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: false },
    societyWorld: { value: input.societyWorld ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.societyWorld.value) {
    accounts.societyWorld.value = await findSocietyWorldPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("task", accounts.task),
      getAccountMeta("societyWorld", accounts.societyWorld),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateSocietyWorldInstructionDataEncoder().encode(
      args as CreateSocietyWorldInstructionDataArgs,
    ),
    programAddress,
  } as CreateSocietyWorldInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountSocietyWorld,
    TAccountSystemProgram
  >);
}

export type CreateSocietyWorldInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountTask extends string = string,
  TAccountSocietyWorld extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  task: Address<TAccountTask>;
  societyWorld: Address<TAccountSocietyWorld>;
  systemProgram?: Address<TAccountSystemProgram>;
  currentTick: CreateSocietyWorldInstructionDataArgs["currentTick"];
  lastSequence: CreateSocietyWorldInstructionDataArgs["lastSequence"];
  lastReceipt: CreateSocietyWorldInstructionDataArgs["lastReceipt"];
  status: CreateSocietyWorldInstructionDataArgs["status"];
  state: CreateSocietyWorldInstructionDataArgs["state"];
};

export function getCreateSocietyWorldInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountTask extends string,
  TAccountSocietyWorld extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
>(
  input: CreateSocietyWorldInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountSocietyWorld,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateSocietyWorldInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity,
  TAccountTask,
  TAccountSocietyWorld,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TASK_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: false },
    societyWorld: { value: input.societyWorld ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("task", accounts.task),
      getAccountMeta("societyWorld", accounts.societyWorld),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateSocietyWorldInstructionDataEncoder().encode(
      args as CreateSocietyWorldInstructionDataArgs,
    ),
    programAddress,
  } as CreateSocietyWorldInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountSocietyWorld,
    TAccountSystemProgram
  >);
}

export type ParsedCreateSocietyWorldInstruction<
  TProgram extends string = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
    task: TAccountMetas[2];
    societyWorld: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateSocietyWorldInstructionData;
};

export function parseCreateSocietyWorldInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateSocietyWorldInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      identity: getNextAccount(),
      task: getNextAccount(),
      societyWorld: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateSocietyWorldInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
