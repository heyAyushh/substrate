/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findReceiptApplicationPda } from "../pdas";
import { TASK_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const SYNC_TASK_STATUS_DISCRIMINATOR = new Uint8Array([
  117, 31, 71, 195, 31, 150, 49, 42,
]);

export function getSyncTaskStatusDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SYNC_TASK_STATUS_DISCRIMINATOR,
  );
}

export type SyncTaskStatusInstruction<
  TProgram extends string = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountReceipt extends string | AccountMeta<string> = string,
  TAccountReceiptApplication extends string | AccountMeta<string> = string,
  TAccountIdentityRegistryProgram extends string | AccountMeta<string> =
    "8ktCGhVZBmjekPXvJhFjiFAqiSRRmBXs3NFHGgkbQKun",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? WritableAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountTask extends string
        ? WritableAccount<TAccountTask>
        : TAccountTask,
      TAccountReceipt extends string
        ? ReadonlyAccount<TAccountReceipt>
        : TAccountReceipt,
      TAccountReceiptApplication extends string
        ? WritableAccount<TAccountReceiptApplication>
        : TAccountReceiptApplication,
      TAccountIdentityRegistryProgram extends string
        ? ReadonlyAccount<TAccountIdentityRegistryProgram>
        : TAccountIdentityRegistryProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SyncTaskStatusInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SyncTaskStatusInstructionDataArgs = {};

export function getSyncTaskStatusInstructionDataEncoder(): FixedSizeEncoder<SyncTaskStatusInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SYNC_TASK_STATUS_DISCRIMINATOR }),
  );
}

export function getSyncTaskStatusInstructionDataDecoder(): FixedSizeDecoder<SyncTaskStatusInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSyncTaskStatusInstructionDataCodec(): FixedSizeCodec<
  SyncTaskStatusInstructionDataArgs,
  SyncTaskStatusInstructionData
> {
  return combineCodec(
    getSyncTaskStatusInstructionDataEncoder(),
    getSyncTaskStatusInstructionDataDecoder(),
  );
}

export type SyncTaskStatusAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountTask extends string = string,
  TAccountReceipt extends string = string,
  TAccountReceiptApplication extends string = string,
  TAccountIdentityRegistryProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  task: Address<TAccountTask>;
  receipt: Address<TAccountReceipt>;
  receiptApplication?: Address<TAccountReceiptApplication>;
  identityRegistryProgram?: Address<TAccountIdentityRegistryProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getSyncTaskStatusInstructionAsync<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountTask extends string,
  TAccountReceipt extends string,
  TAccountReceiptApplication extends string,
  TAccountIdentityRegistryProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SyncTaskStatusAsyncInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountReceipt,
    TAccountReceiptApplication,
    TAccountIdentityRegistryProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SyncTaskStatusInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountReceipt,
    TAccountReceiptApplication,
    TAccountIdentityRegistryProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TASK_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: true },
    task: { value: input.task ?? null, isWritable: true },
    receipt: { value: input.receipt ?? null, isWritable: false },
    receiptApplication: {
      value: input.receiptApplication ?? null,
      isWritable: true,
    },
    identityRegistryProgram: {
      value: input.identityRegistryProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.receiptApplication.value) {
    accounts.receiptApplication.value = await findReceiptApplicationPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      receipt: getAddressFromResolvedInstructionAccount(
        "receipt",
        accounts.receipt.value,
      ),
    });
  }
  if (!accounts.identityRegistryProgram.value) {
    accounts.identityRegistryProgram.value =
      "8ktCGhVZBmjekPXvJhFjiFAqiSRRmBXs3NFHGgkbQKun" as Address<"8ktCGhVZBmjekPXvJhFjiFAqiSRRmBXs3NFHGgkbQKun">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("task", accounts.task),
      getAccountMeta("receipt", accounts.receipt),
      getAccountMeta("receiptApplication", accounts.receiptApplication),
      getAccountMeta(
        "identityRegistryProgram",
        accounts.identityRegistryProgram,
      ),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSyncTaskStatusInstructionDataEncoder().encode({}),
    programAddress,
  } as SyncTaskStatusInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountReceipt,
    TAccountReceiptApplication,
    TAccountIdentityRegistryProgram,
    TAccountSystemProgram
  >);
}

export type SyncTaskStatusInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountTask extends string = string,
  TAccountReceipt extends string = string,
  TAccountReceiptApplication extends string = string,
  TAccountIdentityRegistryProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  task: Address<TAccountTask>;
  receipt: Address<TAccountReceipt>;
  receiptApplication: Address<TAccountReceiptApplication>;
  identityRegistryProgram?: Address<TAccountIdentityRegistryProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getSyncTaskStatusInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountTask extends string,
  TAccountReceipt extends string,
  TAccountReceiptApplication extends string,
  TAccountIdentityRegistryProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
>(
  input: SyncTaskStatusInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountReceipt,
    TAccountReceiptApplication,
    TAccountIdentityRegistryProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SyncTaskStatusInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity,
  TAccountTask,
  TAccountReceipt,
  TAccountReceiptApplication,
  TAccountIdentityRegistryProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TASK_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: true },
    task: { value: input.task ?? null, isWritable: true },
    receipt: { value: input.receipt ?? null, isWritable: false },
    receiptApplication: {
      value: input.receiptApplication ?? null,
      isWritable: true,
    },
    identityRegistryProgram: {
      value: input.identityRegistryProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.identityRegistryProgram.value) {
    accounts.identityRegistryProgram.value =
      "8ktCGhVZBmjekPXvJhFjiFAqiSRRmBXs3NFHGgkbQKun" as Address<"8ktCGhVZBmjekPXvJhFjiFAqiSRRmBXs3NFHGgkbQKun">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("task", accounts.task),
      getAccountMeta("receipt", accounts.receipt),
      getAccountMeta("receiptApplication", accounts.receiptApplication),
      getAccountMeta(
        "identityRegistryProgram",
        accounts.identityRegistryProgram,
      ),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSyncTaskStatusInstructionDataEncoder().encode({}),
    programAddress,
  } as SyncTaskStatusInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountReceipt,
    TAccountReceiptApplication,
    TAccountIdentityRegistryProgram,
    TAccountSystemProgram
  >);
}

export type ParsedSyncTaskStatusInstruction<
  TProgram extends string = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
    task: TAccountMetas[2];
    receipt: TAccountMetas[3];
    receiptApplication: TAccountMetas[4];
    identityRegistryProgram: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: SyncTaskStatusInstructionData;
};

export function parseSyncTaskStatusInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSyncTaskStatusInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      identity: getNextAccount(),
      task: getNextAccount(),
      receipt: getNextAccount(),
      receiptApplication: getNextAccount(),
      identityRegistryProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSyncTaskStatusInstructionDataDecoder().decode(instruction.data),
  };
}
