/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findReceiptApplicationPda } from "../pdas";
import { TASK_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const TASK_RECEIPT_ALREADY_APPLIED_DISCRIMINATOR = new Uint8Array([
  228, 42, 30, 39, 62, 115, 246, 178,
]);

export function getTaskReceiptAlreadyAppliedDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TASK_RECEIPT_ALREADY_APPLIED_DISCRIMINATOR,
  );
}

export type TaskReceiptAlreadyAppliedInstruction<
  TProgram extends string = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountReceipt extends string | AccountMeta<string> = string,
  TAccountReceiptApplication extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? ReadonlyAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountTask extends string
        ? ReadonlyAccount<TAccountTask>
        : TAccountTask,
      TAccountReceipt extends string
        ? ReadonlyAccount<TAccountReceipt>
        : TAccountReceipt,
      TAccountReceiptApplication extends string
        ? ReadonlyAccount<TAccountReceiptApplication>
        : TAccountReceiptApplication,
      ...TRemainingAccounts,
    ]
  >;

export type TaskReceiptAlreadyAppliedInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type TaskReceiptAlreadyAppliedInstructionDataArgs = {};

export function getTaskReceiptAlreadyAppliedInstructionDataEncoder(): FixedSizeEncoder<TaskReceiptAlreadyAppliedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: TASK_RECEIPT_ALREADY_APPLIED_DISCRIMINATOR,
    }),
  );
}

export function getTaskReceiptAlreadyAppliedInstructionDataDecoder(): FixedSizeDecoder<TaskReceiptAlreadyAppliedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getTaskReceiptAlreadyAppliedInstructionDataCodec(): FixedSizeCodec<
  TaskReceiptAlreadyAppliedInstructionDataArgs,
  TaskReceiptAlreadyAppliedInstructionData
> {
  return combineCodec(
    getTaskReceiptAlreadyAppliedInstructionDataEncoder(),
    getTaskReceiptAlreadyAppliedInstructionDataDecoder(),
  );
}

export type TaskReceiptAlreadyAppliedAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountTask extends string = string,
  TAccountReceipt extends string = string,
  TAccountReceiptApplication extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  task: Address<TAccountTask>;
  receipt: Address<TAccountReceipt>;
  receiptApplication?: Address<TAccountReceiptApplication>;
};

export async function getTaskReceiptAlreadyAppliedInstructionAsync<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountTask extends string,
  TAccountReceipt extends string,
  TAccountReceiptApplication extends string,
  TProgramAddress extends Address = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
>(
  input: TaskReceiptAlreadyAppliedAsyncInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountReceipt,
    TAccountReceiptApplication
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  TaskReceiptAlreadyAppliedInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountReceipt,
    TAccountReceiptApplication
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TASK_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    identity: { value: input.identity ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: false },
    receipt: { value: input.receipt ?? null, isWritable: false },
    receiptApplication: {
      value: input.receiptApplication ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.receiptApplication.value) {
    accounts.receiptApplication.value = await findReceiptApplicationPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
      receipt: getAddressFromResolvedInstructionAccount(
        "receipt",
        accounts.receipt.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("task", accounts.task),
      getAccountMeta("receipt", accounts.receipt),
      getAccountMeta("receiptApplication", accounts.receiptApplication),
    ],
    data: getTaskReceiptAlreadyAppliedInstructionDataEncoder().encode({}),
    programAddress,
  } as TaskReceiptAlreadyAppliedInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountReceipt,
    TAccountReceiptApplication
  >);
}

export type TaskReceiptAlreadyAppliedInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountTask extends string = string,
  TAccountReceipt extends string = string,
  TAccountReceiptApplication extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  task: Address<TAccountTask>;
  receipt: Address<TAccountReceipt>;
  receiptApplication: Address<TAccountReceiptApplication>;
};

export function getTaskReceiptAlreadyAppliedInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountTask extends string,
  TAccountReceipt extends string,
  TAccountReceiptApplication extends string,
  TProgramAddress extends Address = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
>(
  input: TaskReceiptAlreadyAppliedInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountReceipt,
    TAccountReceiptApplication
  >,
  config?: { programAddress?: TProgramAddress },
): TaskReceiptAlreadyAppliedInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity,
  TAccountTask,
  TAccountReceipt,
  TAccountReceiptApplication
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TASK_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    identity: { value: input.identity ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: false },
    receipt: { value: input.receipt ?? null, isWritable: false },
    receiptApplication: {
      value: input.receiptApplication ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("task", accounts.task),
      getAccountMeta("receipt", accounts.receipt),
      getAccountMeta("receiptApplication", accounts.receiptApplication),
    ],
    data: getTaskReceiptAlreadyAppliedInstructionDataEncoder().encode({}),
    programAddress,
  } as TaskReceiptAlreadyAppliedInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountReceipt,
    TAccountReceiptApplication
  >);
}

export type ParsedTaskReceiptAlreadyAppliedInstruction<
  TProgram extends string = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
    task: TAccountMetas[2];
    receipt: TAccountMetas[3];
    receiptApplication: TAccountMetas[4];
  };
  data: TaskReceiptAlreadyAppliedInstructionData;
};

export function parseTaskReceiptAlreadyAppliedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTaskReceiptAlreadyAppliedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      identity: getNextAccount(),
      task: getNextAccount(),
      receipt: getNextAccount(),
      receiptApplication: getNextAccount(),
    },
    data: getTaskReceiptAlreadyAppliedInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
