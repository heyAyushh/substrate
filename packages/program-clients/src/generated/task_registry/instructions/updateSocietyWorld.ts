/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findSocietyWorldPda } from "../pdas";
import { TASK_REGISTRY_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_SOCIETY_WORLD_DISCRIMINATOR = new Uint8Array([
  146, 172, 77, 174, 141, 248, 223, 7,
]);

export function getUpdateSocietyWorldDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_SOCIETY_WORLD_DISCRIMINATOR,
  );
}

export type UpdateSocietyWorldInstruction<
  TProgram extends string = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountIdentity extends string | AccountMeta<string> = string,
  TAccountTask extends string | AccountMeta<string> = string,
  TAccountSocietyWorld extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountIdentity extends string
        ? ReadonlyAccount<TAccountIdentity>
        : TAccountIdentity,
      TAccountTask extends string
        ? ReadonlyAccount<TAccountTask>
        : TAccountTask,
      TAccountSocietyWorld extends string
        ? WritableAccount<TAccountSocietyWorld>
        : TAccountSocietyWorld,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateSocietyWorldInstructionData = {
  discriminator: ReadonlyUint8Array;
  currentTick: number;
  lastSequence: bigint;
  lastReceipt: Address;
  status: number;
  state: ReadonlyUint8Array;
};

export type UpdateSocietyWorldInstructionDataArgs = {
  currentTick: number;
  lastSequence: number | bigint;
  lastReceipt: Address;
  status: number;
  state: ReadonlyUint8Array;
};

export function getUpdateSocietyWorldInstructionDataEncoder(): Encoder<UpdateSocietyWorldInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["currentTick", getU32Encoder()],
      ["lastSequence", getU64Encoder()],
      ["lastReceipt", getAddressEncoder()],
      ["status", getU8Encoder()],
      ["state", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_SOCIETY_WORLD_DISCRIMINATOR,
    }),
  );
}

export function getUpdateSocietyWorldInstructionDataDecoder(): Decoder<UpdateSocietyWorldInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["currentTick", getU32Decoder()],
    ["lastSequence", getU64Decoder()],
    ["lastReceipt", getAddressDecoder()],
    ["status", getU8Decoder()],
    ["state", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
  ]);
}

export function getUpdateSocietyWorldInstructionDataCodec(): Codec<
  UpdateSocietyWorldInstructionDataArgs,
  UpdateSocietyWorldInstructionData
> {
  return combineCodec(
    getUpdateSocietyWorldInstructionDataEncoder(),
    getUpdateSocietyWorldInstructionDataDecoder(),
  );
}

export type UpdateSocietyWorldAsyncInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountTask extends string = string,
  TAccountSocietyWorld extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  task: Address<TAccountTask>;
  societyWorld?: Address<TAccountSocietyWorld>;
  currentTick: UpdateSocietyWorldInstructionDataArgs["currentTick"];
  lastSequence: UpdateSocietyWorldInstructionDataArgs["lastSequence"];
  lastReceipt: UpdateSocietyWorldInstructionDataArgs["lastReceipt"];
  status: UpdateSocietyWorldInstructionDataArgs["status"];
  state: UpdateSocietyWorldInstructionDataArgs["state"];
};

export async function getUpdateSocietyWorldInstructionAsync<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountTask extends string,
  TAccountSocietyWorld extends string,
  TProgramAddress extends Address = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
>(
  input: UpdateSocietyWorldAsyncInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountSocietyWorld
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateSocietyWorldInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountSocietyWorld
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TASK_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: false },
    societyWorld: { value: input.societyWorld ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.societyWorld.value) {
    accounts.societyWorld.value = await findSocietyWorldPda({
      task: getAddressFromResolvedInstructionAccount(
        "task",
        accounts.task.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("task", accounts.task),
      getAccountMeta("societyWorld", accounts.societyWorld),
    ],
    data: getUpdateSocietyWorldInstructionDataEncoder().encode(
      args as UpdateSocietyWorldInstructionDataArgs,
    ),
    programAddress,
  } as UpdateSocietyWorldInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountSocietyWorld
  >);
}

export type UpdateSocietyWorldInput<
  TAccountAuthority extends string = string,
  TAccountIdentity extends string = string,
  TAccountTask extends string = string,
  TAccountSocietyWorld extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  identity: Address<TAccountIdentity>;
  task: Address<TAccountTask>;
  societyWorld: Address<TAccountSocietyWorld>;
  currentTick: UpdateSocietyWorldInstructionDataArgs["currentTick"];
  lastSequence: UpdateSocietyWorldInstructionDataArgs["lastSequence"];
  lastReceipt: UpdateSocietyWorldInstructionDataArgs["lastReceipt"];
  status: UpdateSocietyWorldInstructionDataArgs["status"];
  state: UpdateSocietyWorldInstructionDataArgs["state"];
};

export function getUpdateSocietyWorldInstruction<
  TAccountAuthority extends string,
  TAccountIdentity extends string,
  TAccountTask extends string,
  TAccountSocietyWorld extends string,
  TProgramAddress extends Address = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
>(
  input: UpdateSocietyWorldInput<
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountSocietyWorld
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateSocietyWorldInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountIdentity,
  TAccountTask,
  TAccountSocietyWorld
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TASK_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    identity: { value: input.identity ?? null, isWritable: false },
    task: { value: input.task ?? null, isWritable: false },
    societyWorld: { value: input.societyWorld ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("identity", accounts.identity),
      getAccountMeta("task", accounts.task),
      getAccountMeta("societyWorld", accounts.societyWorld),
    ],
    data: getUpdateSocietyWorldInstructionDataEncoder().encode(
      args as UpdateSocietyWorldInstructionDataArgs,
    ),
    programAddress,
  } as UpdateSocietyWorldInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountIdentity,
    TAccountTask,
    TAccountSocietyWorld
  >);
}

export type ParsedUpdateSocietyWorldInstruction<
  TProgram extends string = typeof TASK_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    identity: TAccountMetas[1];
    task: TAccountMetas[2];
    societyWorld: TAccountMetas[3];
  };
  data: UpdateSocietyWorldInstructionData;
};

export function parseUpdateSocietyWorldInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateSocietyWorldInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      identity: getNextAccount(),
      task: getNextAccount(),
      societyWorld: getNextAccount(),
    },
    data: getUpdateSocietyWorldInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
